# 任务清单

每个 task 跑的时候维护一份。完成的项移到 `## Done`。

---

## 高优先级

（暂无）

## 低优先级

- **V2 P1：模型管理页 Diff 视图**（`docs/05-前端设计/前端V2界面与功能优化方案.md` §P1）：models 列表加 "比较" 模式，把 capabilities/inputs/outputs/cost 横向并列；待真实模型数超过 5 个再做。
- **V2 P1：Usage 页 Provider × Model 矩阵热力图**：当前是表格，方案文档建议改成热力图作为概览，表格降为 Drill-down。
- **V2 P2：API Keys 多 key 轮转 + 用量配额**：当前一 provider 一 key，方案建议支持多 key 池 + 每 key 用量上限；需要后端 schema 改造。
- **Playwright 集成测试套件**：当前只有 4 个 mock 化 smoke spec；如果想做真后端 + 真数据库的 E2E，需要单独 `e2e/integration/` + CI 矩阵。
- **`/api-keys` 高亮 deep-link 视觉**：当前 `?provider=xxx` 用 primary 边框，未来可以改成更显眼的 toast / pulse 动画。

## Done

- [x] **当前任务：修复 Docker build `useSearchParams` Suspense 报错**（2026-06-07）：1 个文件 12 行改动（`apps/web/src/app/activity/page.tsx` 加 `<Suspense fallback={null}>`），与 `/workspace` / `/api-keys` 同构。`npm run build` 与 `docker compose build` 全 5 服务通过。设计决策 D-2026-06-07-01 见 `设计决策.md`。详见 `进度跟踪.md` 末尾。
- [x] **上一任务：V2 前端 P0 改造**（2026-06-06）：9 个 stage 全完成。改造范围 P0-1..P0-6 + Sidebar footer v2.0；后端新增 `POST /api/providers/{id}/test`（9/9 pytest），前端新增 `/api-keys` + `/activity` 路由 + `StatusPill` 组件 + Output Tabs 重构；Playwright 框架从零搭建 + 4 个 V2 spec。设计决策 D-2026-06-06-01..04 见 `设计决策.md`。详见 `进度跟踪.md` 末尾。
- [x] **上一任务：GitHub README 中英双语 + 4 张配图嵌入**（2026-06-04）：5 个 stage 全完成。`README.md`（英文）保留默认入口、新增 `README.zh-CN.md`（中文），4 张截图集中放 Screenshots 章节。`docker compose up --build` 路径可用性已验证。设计决策 D-2026-06-04-10..13 见 `设计决策.md`。
- [x] **更上一任务：整理 `docs/` 结构 + 补 `.gitignore` 防止 IP 泄露**（2026-06-04）：8 个 stage 全完成。最终布局见 `进度跟踪.md` 末尾。设计决策 D-2026-06-04-06..09 见 `设计决策.md`。
- [x] **更上一任务：正规化 `GET /usage/models` 的 providerId 处理**（2026-06-04）：详见 `设计决策.md` D-2026-06-04-01..05 和 git log。
