# ModelGate 待开发文档

更新时间：2026-06-01

本文档汇总两类内容：

1. 前端按钮 / 可交互控件与后端接口的联通审计结果。
2. `项目总TODO.md` 中仍未完成的开发项。

---

## 1. 前端按钮与后端联通审计

审计范围：

- 当前实际入口：`apps/web/src/components/workspace/workspace-shell.tsx`
- 其他现有页面：`history-client.tsx`、`model-management.tsx`、`settings-client.tsx`
- 后端路由：`apps/server/app/api/*`

### 1.1 已联通后端的核心交互

| 前端交互 | 当前行为 | 后端接口 | 状态 |
|---|---|---|---|
| 工作台初始化 Provider | 页面加载读取 Provider | `GET /api/providers` | 已联通 |
| 工作台初始化模型 | 页面加载读取模型列表 | `GET /api/models` | 已联通 |
| 任务类型切换 | 更新 `taskType` 与 URL，并触发模型推荐 query key 变化 | `POST /api/models/recommend` | 已联通 |
| Provider 全部 / 单 Provider 筛选 | 更新 `providerFilter`，触发模型推荐 | `POST /api/models/recommend` | 已联通 |
| 选择模型卡片 | 更新 `selectedModelId`，读取对应参数 Schema | `GET /api/param-schemas/{schemaId}` | 已联通 |
| 动态参数输入 / slider / checkbox / select | 更新本地参数，并参与推荐与运行请求 | `POST /api/models/recommend`、`POST /api/chat/runs` | 已联通 |
| 上传文件按钮 | 打开文件选择器，选择文件后上传 | `POST /api/files/upload` | 已联通 |
| 清除文件按钮 | 删除当前文件记录 | `DELETE /api/files/{fileId}` | 已联通 |
| 运行按钮 | 创建真实 Chat Run | `POST /api/chat/runs` | 已联通 |
| 输出预览 | 展示最新 run 输出或错误 requestId | 来自 `POST /api/chat/runs` 响应 | 已联通 |
| 结果归档 tab | 展示工作台已加载历史 | `GET /api/history/runs` | 已联通 |
| 历史记录页 | 读取运行历史与请求日志 | `GET /api/history/runs`、`GET /api/logs/requests` | 已联通 |
| 模型管理页 | 读取 Provider 与模型 | `GET /api/providers`、`GET /api/models` | 已联通 |
| 设置页 Provider Key 状态 | 只读展示 Provider env key 和启用状态 | `GET /api/providers` | 已联通读能力 |

### 1.2 未联通或仍为占位的前端交互

| 前端位置 / 控件 | 当前状态 | 缺口 | 建议后续处理 |
|---|---|---|---|
| 左侧导航：任务中心 | 纯视觉按钮，无 `onClick` / `href` | 未跳转到工作台首页 | 改为 `/workspace?taskType=chat` |
| 左侧导航：Chat Runtime | 纯视觉按钮 | 未跳转、未筛选 Chat 任务 | 改为当前工作台 Chat 视图或独立 Runtime 页 |
| 左侧导航：Generation Runtime | 纯视觉按钮 | 未调用 generation 页面或 API | 接入 `/api/generation/tasks` 前先禁用或标注后续 |
| 左侧导航：历史记录 | 纯视觉按钮 | 已有 `/history` 页面但未链接 | 改为 `Link href="/history"` |
| 左侧导航：请求日志 | 纯视觉按钮 | 已有日志接口但无独立页面 / 路由 | 可跳转 `/history#logs` 或新增 `/logs` |
| 左侧导航：Provider 管理 | 纯视觉按钮 | 无 Provider 管理页面 | 第一版可链接 `/settings`，后续新增管理页 |
| 左侧导航：模型注册表 | 纯视觉按钮 | 已有 `/models` 页面但未链接 | 改为 `Link href="/models"` |
| 左侧导航：工作流 | 纯视觉按钮 | Workflow Runtime 尚未实现 | 保持禁用并写明后续 |
| 左侧导航：设置 | 纯视觉按钮 | 已有 `/settings` 页面但未链接 | 改为 `Link href="/settings"` |
| 顶部搜索框 | 静态展示，无输入 | 未搜索任务、模型、文件、日志 | 新增全局搜索前端状态；后端可复用 `/models`、`/history/runs`、`/logs/requests`，或新增 search API |
| `API Key 配置` 按钮 | 无点击逻辑 | 后端没有 API Key 写入接口 | 第一版建议跳转 `/settings`；后续如支持 UI 写 key，需要新增安全设计和后端接口 |
| 顶部下拉箭头 | 无点击逻辑 | 没有菜单动作 | 删除、禁用，或接入运行选项菜单 |
| `新建任务` 按钮 | 仅本地 reset workspace | 不创建后端任务记录 | 当前可接受；如需要任务草稿，需要新增 draft/task API |
| 图片理解任务按钮 | disabled | 前端未开放，Chat Runtime 尚未注入图片二进制 / base64 | 接入视觉模型后启用 |
| 视频理解任务按钮 | disabled | 缺视频抽帧与多模态输入 | 完成视频抽帧和视觉模型后启用 |
| 文生图 / 图生图 / 文生视频 / 图生视频 / 首尾帧视频按钮 | disabled | 前端未调用 `/api/generation/tasks` | 接入 Generation Runtime UI 和真实 Provider 后启用 |
| 多 Agent 工作流按钮 | disabled | Workflow Runtime 未实现 | Phase 10 后续开发 |
| Prompt 区 `示例` 按钮 | 只填充本地示例 | 不需要后端，但当前没有模板库 | 后续 Prompt 模板库实现后可联通模板 API |
| 输出区 `全屏` | 静态文字，不是按钮 | 无前端功能和后端关系 | 做成本地全屏预览或移除 |
| 参数面板 `重置` | 有按钮，无 `onClick` | 不能恢复 schema 默认值 | 本地重置到 `param-schemas` 默认值；不需要后端写接口 |
| 静态参数 `reasoning_mode` | 纯展示 | 未进入真实 params | 改为 Schema 驱动字段，或从 UI 移除 |
| 静态参数 `output_format` | 纯展示 | 未进入真实 params | 改为 Schema 驱动字段，或从 UI 移除 |
| 静态参数 `image_size` | 纯展示 | Generation 未启用，未提交 | 接入 generation schema 后启用 |
| 静态参数 `async_mode` | 纯展示 | 未参与请求 | Chat 默认同步；Generation 接入后由真实 schema 控制 |
| 静态参数 `webhook_url` | 纯展示 | 后端暂无 webhook 回调 | 新增 webhook 设计前应移除或禁用 |
| 历史表格旋转图标 | 只是图标，不可点击 | 没有重跑动作 | 接入重跑：Chat 用原 run 参数重新 `POST /api/chat/runs`；Generation 用 `POST /api/generation/tasks/{taskId}/rerun` |
| 请求日志表格 | 只读表格 | 没有详情查看 | 可新增日志详情抽屉或链接到 `/api/logs/requests` 过滤结果 |
| Provider 状态延迟 | 使用静态 `32ms/50ms/68ms` | 不是真实健康检查 | 新增 Provider health / latency 接口，或显示配置状态而非延迟 |
| 工作流步骤条 | 静态流程展示 | 不反映真实运行阶段 | 后续接入 run events / generation task 状态 |

### 1.3 旧组件状态

以下组件仍在仓库中，但当前新版工作台入口没有直接使用，不能代表当前页面的按钮联通状态：

- `apps/web/src/components/workspace/task-center.tsx`
- `apps/web/src/components/workspace/file-uploader.tsx`
- `apps/web/src/components/workspace/model-selector.tsx`
- `apps/web/src/components/workspace/result-panel.tsx`
- `apps/web/src/components/workspace/dynamic-param-form.tsx`

后续建议：

- 如果确定新版 `WorkspaceShell` 是唯一工作台入口，清理旧组件或改造成可复用子组件。
- 如果保留旧组件，需要补 Story / E2E，避免旧按钮能力和新版页面能力不一致。

---

## 2. 从项目总 TODO 汇总的未完成项

### 2.1 文件与多模态增强

- [ ] 视频抽帧。
- [ ] 图片理解：将图片二进制或 base64 注入多模态 Provider。
- [ ] 视频理解：依赖视频抽帧、视频 metadata 和视觉模型输入。
- [ ] 上传图片并推荐图片理解模型的 E2E 验收。

说明：`BEGIN_USER_FILE_CONTEXT` 文本文件边界在 `chat_runtime.py` 中已实现，后续不再作为待开发项；图片 / 视频多模态仍未完成。

### 2.2 Chat Runtime 增强

- [ ] 流式输出。
- [ ] 运行中 Abort / cancel。
- [ ] 流式输出测试。

### 2.3 Generation Runtime 与 Provider 接入

- [ ] 结果文件下载。
- [ ] 输出文件持久化。
- [ ] 火山 Seedance 文生视频。
- [ ] 火山 Seedance 图生视频。
- [ ] MiniMax 图像或视频生成能力。
- [ ] 其他 Generation Provider 预留。
- [ ] 上传图片并创建图生视频任务的真实 E2E 验收。

### 2.4 幂等、限流与轮询

- [ ] 相同任务短时间重复提交提示。
- [ ] Provider rate limit 配置。
- [ ] 轮询频率配置。

### 2.5 安全与权限

- [ ] 下载接口鉴权。
- [ ] 预览接口鉴权。
- [ ] 输出文件不可猜测 ID。
- [ ] 删除文件权限校验。
- [ ] Workflow 节点输出标记来源。
- [ ] 模型输出不能修改系统配置。

说明：第一版是本地单用户无登录，鉴权类事项不阻塞本地版上线，但需要在多用户版本前完成。

### 2.6 Workflow Runtime

- [ ] workflow_definitions 数据结构可用。
- [ ] workflow_runs 数据结构可用。
- [ ] DAG 节点定义。
- [ ] 节点输入输出映射。
- [ ] 节点执行状态。
- [ ] 工作流运行历史。
- [ ] 第一版不开放自动执行本地命令。

### 2.7 多模型与批量能力

- [ ] 多模型结果对比。
- [ ] 批量生成。
- [ ] 自动 fallback。
- [ ] Prompt 模板库。
- [ ] 成本统计面板。
- [ ] Provider 健康检查。

### 2.8 外部集成

- [ ] OpenAI-compatible endpoint 设计。
- [ ] MCP Server 设计。
- [ ] 外部 API 鉴权。
- [ ] 工具能力白名单。
- [ ] 外部调用限流。
- [ ] 外部调用审计日志。

### 2.9 多用户版本

- [ ] 用户认证。
- [ ] session / token 管理。
- [ ] user_id 数据隔离。
- [ ] 用户级 Provider Key。
- [ ] 文件下载权限。
- [ ] 历史记录权限。
- [ ] 管理员和普通用户权限。

---

## 3. 建议优先级

### P0：第一版上线前建议处理

- [ ] 左侧导航接真实页面链接：`/workspace`、`/history`、`/models`、`/settings`。
- [ ] `API Key 配置` 按钮跳转设置页或禁用并说明。
- [ ] 参数面板 `重置` 按钮恢复 schema 默认值。
- [ ] 移除或禁用没有真实行为的顶部下拉、全屏、历史重跑图标。
- [ ] 静态参数字段改为真实 schema 字段，或从 UI 中移除。
- [ ] Provider 状态延迟去掉假数据，改为配置状态或真实健康检查。

### P1：第一版后增强

- [ ] Chat 流式输出和运行中取消。
- [ ] 图片多模态输入。
- [ ] Generation Runtime 前端页面接入 `/api/generation/tasks`。
- [ ] 历史记录详情、重跑、删除。
- [ ] 请求日志详情与过滤。
- [ ] Prompt 模板库。

### P2：后续版本

- [ ] Workflow Runtime。
- [ ] 多模型对比、批量、fallback。
- [ ] OpenAI-compatible endpoint 和 MCP Server。
- [ ] 多用户、权限、用户级 Provider Key。
