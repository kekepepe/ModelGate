# 进度跟踪

每个 task 跑的时候维护一份。所有 stage 完成后这个文件**保留**作为历史日志，不删除。

---

## 上一任务：正规化 `GET /usage/models` 的 providerId 处理（2026-06-04 完成）

详见 `docs/04-开发管理/设计决策.md` 的 D-2026-06-04-01..05。代码改动：`apps/server/app/api/usage.py::get_usage_models` + 新增 `tests/test_usage_models_phase10.py`（8/8 通过）。

---

## 上一任务：整理 `docs/` 结构 + 补 `.gitignore` 防止 IP 泄露（2026-06-04 完成）

仓库根目录散落着 6 份设计/项目 md 和 2 个目录（`前端设计参考图/`、`tokenplan_api文档/`），命名不一致，并且其中 3 类内容（第三方截图、厂商 API 文档、内部评估反馈）不应推到 GitHub。统一整理：

- 把原创设计/项目文档归入 `docs/`，新建 `docs/05-前端设计/`
- 把第三方/内部敏感内容本地保留，加进 `.gitignore`
- 把工作流 tracking 文件改中文命名并归入 `docs/04-开发管理/`

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 0 | 切到本任务，更新 tracking doc | done |
| 1 | 建 `docs/05-前端设计/`，git mv 4 份前端设计 md | done |
| 2 | git mv `项目总TODO.md` → `docs/04-开发管理/项目总TODO.md` | done |
| 3 | 改名 PROGRESS / DECISIONS / TODO 为中文，归入 `docs/04-开发管理/` | done |
| 4 | `.gitignore` 追加 3 条 IP 保护 pattern | done |
| 5 | `git rm -r` 被 gitignore 的 3 类文件（保留本地） | done |
| 6 | `docs/README.md` 加 `05-前端设计/` 索引 + 列表 gitignore 提示 | done |
| 7 | 跑 pytest 兜底（8/8 必须仍然过） | done |
| 8 | 收尾 tracking doc | done |

### Stage log

#### Stage 0 — 2026-06-04
- 切到新任务目标。
- 决定工作流文件中文命名（`进度跟踪.md` / `设计决策.md` / `任务清单.md`），归到 `docs/04-开发管理/`。
- 用户在前置问题中确认 3 类敏感内容（截图、厂商 API 文档、内部评估）一律 gitignore + 保留本地。

#### Stage 1 — 2026-06-04
- 建 `docs/05-前端设计/`，3 份 tracked 文件用 `git mv`，1 份 untracked 用 `mv`。
- 命名规范化：
  - `ModelGate_前端视觉参考改造计划.md` → `docs/05-前端设计/前端视觉参考改造计划.md`（去 `ModelGate_` 前缀）
  - `ModelGate_前端页面设计.md` → `docs/05-前端设计/前端页面设计.md`（去 `ModelGate_` 前缀）
  - `前端usage页面设计.md` → `docs/05-前端设计/前端Usage页面设计.md`（小写 usage 改大写）
  - `前端配色设计.md` → `docs/05-前端设计/前端配色设计.md`
- `docs/05-前端设计/前端视觉参考改造计划.md` 中第 3 行对 `前端设计参考图/` 的引用改写为"本地目录，gitignore 内"，避免读者按路径找不到文件。

#### Stage 2 — 2026-06-04
- `git mv 项目总TODO.md docs/04-开发管理/项目总TODO.md`。

#### Stage 3 — 2026-06-04
- 3 份文件未 tracked，plain `mv`：
  - `docs/PROGRESS.md` → `docs/04-开发管理/进度跟踪.md`
  - `docs/DECISIONS.md` → `docs/04-开发管理/设计决策.md`
  - `docs/TODO.md` → `docs/04-开发管理/任务清单.md`
- `CLAUDE.md` 和 `memory/feedback_staged_execution.md` 中对旧名的引用同步更新。

#### Stage 4 — 2026-06-04
- `.gitignore` 追加 3 条：
  - `**/前端设计参考图/`
  - `**/tokenplan_api文档/`
  - `*-修改建议.md`
- 验证：`git check-ignore --no-index minimax3-修改建议.md` 命中；新建 `test-修改建议.md` 也命中（说明 pattern 正确，未来类似文件会被 ignore）。

#### Stage 5 — 2026-06-04
- `git rm -r --cached 前端设计参考图/` 5 个 PNG
- `git rm -r --cached tokenplan_api文档/` 4 个 md
- `git rm --cached minimax3-修改建议.md`
- 验证本地磁盘：`minimax3-修改建议.md` 和 `tokenplan_api文档/` 仍在；`前端设计参考图/` 在工作树已被用户物理删除（Stage 5 之前），新位置 `前端设计/前端设计参考图/` 走 gitignore 路径。

#### Stage 6 — 2026-06-04
- 重写 `docs/README.md`：
  - 加目录表（前缀索引 + 主题）
  - `04-开发管理/` 列全：开发规范、测试策略、Phase 9 验收、待开发、前端联通审计、Celery 替代、项目总 TODO、进度跟踪、设计决策、任务清单
  - 新加 `05-前端设计/` section
  - 新加"本地保留、不上 GitHub 的内容"小节，列 3 个被 ignore 的路径

#### Stage 7 — 2026-06-04
- `pytest tests/test_usage_models_phase10.py -v` 仍然 8/8 通过（0.58s）。
- 兜底确认：移动/改名文档不影响 Python 测试。

#### Stage 8 — 2026-06-04
- 当前文件（progress / decision / todo）收尾，记录 stage log。

### 最终目录布局

```
docs/
├── 00-项目概览/
├── 01-产品需求/
├── 02-技术设计/
├── 03-安全与风险/
├── 04-开发管理/
│   ├── Celery开发模式与替代方案.md
│   ├── Phase9测试与验收清单.md
│   ├── 测试策略文档.md
│   ├── 待开发文档.md
│   ├── 进度跟踪.md
│   ├── 开发规范文档.md
│   ├── 前端联通审计.md
│   ├── 任务清单.md
│   ├── 设计决策.md
│   └── 项目总TODO.md
├── 05-前端设计/
│   ├── 前端Usage页面设计.md
│   ├── 前端配色设计.md
│   ├── 前端视觉参考改造计划.md
│   └── 前端页面设计.md
└── README.md
```

仓库根目录从 6 份散落 md + 2 个散落目录清空。

### 已知 / 待跟进
- 当前没有未完成项。如未来有"3 类敏感内容"的新文件，直接被 gitignore 自动 catch。

---

## 当前任务：GitHub README 中英双语 + 4 张界面截图嵌入（2026-06-04）

用户提供了 4 张产品截图（Overview / Playground / Models / Usage）放在
`github-picture/`，希望：(a) 嵌入到 README 合适位置，(b) GitHub 仓库
上提供中英两个 README，(c) 顺便确认他人 `git clone` + `docker compose
up --build` 是否可用。

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 0 | 重读 5 份上下文（README / CLAUDE / 3 tracking） | done |
| 1 | 校验 `docker compose config`、Dockerfile 链路、`.env` 必填性、截图 gitignore 状态 | done |
| 2 | 询问用户确认 README 默认语言（英文 vs 中文） | done |
| 3 | 改写 `README.md`（英文），在 Stack 和 Quick start 之间新增 Screenshots 章节 | done |
| 4 | 新增 `README.zh-CN.md`（中文），与英文同构 | done |
| 5 | 更新 3 份 tracking 文档（进度跟踪 / 设计决策 / 任务清单） | done |

### Stage log

#### Stage 0 — 2026-06-04
- 重读 `README.md`（旧版纯英文，无图）、`CLAUDE.md`、3 份 `docs/04-开发管理/*.md`。
- 用户意图明确：截图嵌入、双语 README、确认 docker compose 可用性。

#### Stage 1 — 2026-06-04（前置验证）
- `docker compose config` exit 0，5 服务（postgres / redis / api / worker / web）配置语法正确，healthcheck 与 depends_on 链完整。
- `apps/server/Dockerfile` 与 `apps/web/Dockerfile` 都在；web Dockerfile 多阶段构建正确复制 `apps/web/package.json`、`src/.next/next.config.ts`。
- `MODELGATE_SECRET_KEY` 在 `APP_ENV=development` 下可为空（fallback `DEV_SECRET_MATERIAL`），仅 production 才 raise RuntimeError。
- `.env` 仍在 `.gitignore` 内，新用户**必须** `cp .env.example .env`；API Key 全空也能起，UI 可浏览，所有 provider 显示 "No Key"。
- `github-picture/` 4 张 PNG（173K / 122K / 194K / 202K）都不在 `.gitignore` 内，会被自然 track。
- 结论：他人在 GitHub 克隆后执行 `cp .env.example .env && docker compose up --build` 即可启动，UI 立即可看；要做真实推理至少填一个 API Key。

#### Stage 2 — 2026-06-04
- 通过 AskUserQuestion 锁定 README 默认语言为**英文**（与当前 README 内容保持一致，国际访客首屏更友好），中文版放 `README.zh-CN.md`。

#### Stage 3 — 2026-06-04（英文 README 改写）
- `README.md` 在 Stack 与 Quick start 之间新增 **Screenshots** 章节，4 张图按页面语义放置：
  - `github1.png` → Overview（总览）
  - `github2.png` → Playground（实验场）
  - `github3.png` → Models（模型）
  - `github4.png` → Usage（用量分析）
- 顶部加语言切换链接 `**[English](README.md)** · **[简体中文](README.zh-CN.md)**`。
- Quick start 章节加一段提示，告知"key 全空也能起、UI 可浏览"。
- 现有 Why / Scope / Architecture / Stack / Verification / Documentation / License 章节保留原文。

#### Stage 4 — 2026-06-04（中文 README 新建）
- `README.zh-CN.md` 与英文版同构：标题、Why、Scope、Architecture、Stack、Screenshots、Quick start、Verification、Documentation、License。
- 代码块（docker compose、conda、pytest 等命令）**不翻译**，仅翻译描述性 prose；专有名词（Provider、Capability Router、JSON Schema、HKDF、AES-256-GCM、SSE 等）保留英文。
- 4 张图路径与英文版完全一致（`github-picture/github{1..4}.png`）。
- 自检修复：Models 段第一次写入时误把图写成 `github1.png`，已 `Edit` 修正为 `github3.png`。

#### Stage 5 — 2026-06-04（tracking 收尾）
- `进度跟踪.md`：新增"当前任务" section 记录本任务 5 个 stage 与结论。
- `设计决策.md`：新增 D-2026-06-04-10..13，4 条决策（双语 README、截图路径、Screenshots 章节位置、不改 .gitignore）。
- `任务清单.md`：本任务入 Done 列表。
- 范围限定：本任务**纯文档变更**，未触任何 `app/*`、`tests/*`、`.env*`、`docker-compose.yml`，按 staged-execution 规则不补 pytest（"Ship an automated check (pytest) for any backend change" 不适用）。

### 最终仓库根目录 README 布局

```
README.md           # 英文，含 4 张图、顶部语言切换链接
README.zh-CN.md     # 简体中文，与 README.md 同构
github-picture/     # 4 张界面截图
  ├── github1.png   # Overview
  ├── github2.png   # Playground
  ├── github3.png   # Models
  └── github4.png   # Usage
```

### 已知 / 待跟进
- 无。"他人 git clone + docker compose up --build" 的可用性已验证，README 已在英文+中文两个版本就位。后续如要往 README 补 demo GIF / 视频，再开新任务。

---

## 当前任务：V2 前端 P0 改造（2026-06-06 起）

按 `docs/05-前端设计/前端V2界面与功能优化方案.md` Phase V2.1 + V2.2，仅做 P0 六项 + Sidebar 版本号。

**范围锁定（用户 2026-06-06 确认）：**
- 只做 P0-1..P0-6 + footer v1.0 → v2.0；P1/P2 不做。
- P0-4 Test Connection：后端只加 `POST /api/providers/{id}/test`，不动 `provider_secrets` 表；前端用 sessionStorage 记忆 last test。
- Playwright 零基础，本次从零搭框架 + 4 个 V2 新页面 spec。
- 前后端联通：pytest + Playwright + 启 next dev 实测。

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 0 | 重读 5 份文档 + 前后端现状扫描（两 Explore agent 并行）| done |
| 1 | P0-1：ModelSelectorRow 增强（Provider Ready / Match / Cost / Speed / Details） | done |
| 2 | P0-2：Output 区 Tabs 重构 Output/Timeline/Request/Archive + 失败可操作建议 | done |
| 3 | P0-3：新建 `/activity` 合并 history + logs，详情抽屉复用 `/usage/logs/{id}` | done |
| 4 | P0-4：后端 `POST /api/providers/{id}/test` 轻量实现 + pytest；前端 Test 按钮 | done |
| 5 | P0-5：`StatusPill` 组件 + 全站替换散落 Badge | done |
| 6 | P0-6：新建 `/api-keys` 独立路由，从 Settings 迁出 Provider Key 模块 | done |
| 7 | Sidebar footer v1.0 → v2.0 | done |
| 8 | 前后端联通验证：pytest 全量 + Playwright 4 spec + next dev 实测 | done |
| 9 | 收尾 3 份 tracking docs | done |

### Stage log

#### Stage 0 — 2026-06-06
- 重读 `README.md`、`CLAUDE.md`、`进度跟踪.md`/`设计决策.md`/`任务清单.md`、V2 方案 778 行。
- 并行跑 2 个 Explore agent 扫前端 + 后端。
- 前端关键命中：`sidebar.tsx:144` v1.0 footer；`workspace/model-selector-row.tsx` 当前极简；`output-section.tsx:33-35` Tabs 是 Output/Status/Archive；`history-client.tsx` + `activity-logs-page.tsx` 两页数据源都是 `/history/runs`，合并合理；`settings-client.tsx` 无 Tab，Provider Key 模块需要迁出；无 `Test connection` 按钮；Playwright 完全未配置；`components/ui/` 缺 `dialog`/`table`/`dropdown-menu`（Radix 依赖已在 deps 可直接 add）。
- 后端关键命中：`providers.py` 已有 GET/PUT/DELETE，无 Test endpoint；Adapter Protocol（`providers/base.py:97-117`）无 `health_check`，本次走"复用 chat + 1 token 最小探测"路径，不改 Protocol；`usage.py:510` 的 `_serialize_usage_log` 已聚合 run + request_log，Activity 详情抽屉可直接复用；测试 fixture 标准范例 `tests/test_usage_models_phase10.py`（SQLite StaticPool + `app.dependency_overrides[get_db]`）。
- 范围确认：用户选 P0 only + P0-4 轻量 + Playwright 4 spec + next dev 实测。

#### Stage 1 — 2026-06-06
- 重写 `apps/web/src/components/workspace/model-selector-row.tsx`：Provider Ready / Match / Cost / Speed 4 块徽章，Available / Unavailable 分组下拉，Details Sheet 抽屉展示 capabilities/inputs/outputs/latency，调用 `/api/usage/models` 取近 7 天延迟+成功率拼接到模型行。
- 修 `workspace-shell.tsx`：向新 selector 传入 `hiddenModels` / `providers` / `taskInputTypes`。

#### Stage 2 — 2026-06-06
- 重写 `apps/web/src/components/workspace/output-section.tsx`：新增 Tabs Output / Timeline / Request / Archive。
- 新组件：`RunTimeline`（Queued→Validating→Calling→Receiving→Saving 5 步状态机）、`RequestSummary`（Status/Provider/Model/TaskType/OutputType/RunId/RequestId + 复制按钮）、`ErrorBanner`（按 `errorType` 推荐操作；`PROVIDER_AUTH_*` 链 `/api-keys?provider={id}`，失败可点 "Open log" 跳 `/activity?runId={id}`）。

#### Stage 3 — 2026-06-06
- 新建 `/activity` 路由 + `components/activity/activity-page.tsx`：单表合并 runs + request_logs，列 Time/Type/Task/Provider/Model/Status/Latency/RecordId/Actions。
- 过滤器：search/type/provider/status；从 `?runId=` 自动填 search 实现深链。
- 详情抽屉复用 `/api/usage/logs/{log_id}` 单接口。
- `apps/web/src/app/history/page.tsx` 与 `/logs/page.tsx` 全部改为 `redirect("/activity")`，保持旧链接不死。
- Sidebar `/logs` 入口 → `/activity`；`isActive` 用 `pathname === base || pathname.startsWith(base + "/")` 修复误匹配。

#### Stage 4 — 2026-06-06
- `apps/server/app/api/providers.py` 新增 `POST /{provider_id}/test`：选第一个 enabled 的 `runtime ∈ {chat, chat_completion}` 模型，通过 `create_chat_adapter` 发 1-token 探测；`_classify_probe_error` 把 `AppError.error_type` 映射到 `auth_failed / rate_limited / timeout / unreachable / forbidden / bad_request / server_error / request_error / config_error`，未知归 `error`。
- 响应固定字段：`{providerId, status, errorType?, message?, modelId?}`，前端可直接渲染。
- 新增 `tests/test_provider_test_phase11.py`：9 条用例（missing_key / no_chat_model 404 / ok / auth_failed / rate_limited / unreachable / timeout / server_error / 响应 shape 契约）。Fixture 模式与 phase10 一致 + 额外 monkeypatch `startup_module.engine`、`startup_module.SessionLocal`、`provider_secrets_module.SessionLocal`，否则 lifespan 拿到的是 import-time 绑定的 Postgres engine 而不是 SQLite。9/9 通过。
- 修复：第一次跑测试时发现 `_pick_probe_model` 只匹配 `runtime == "chat"`，但 mimo 注册的是 `chat_completion`，导致全 `no_chat_model`；放宽为白名单。

#### Stage 5 — 2026-06-06
- 之前的 Stage 1/2/3 已经把工作区、Output、Activity 的状态色都改用 `StatusPill`；这阶段补 sidebar 的 Provider 状态块。`components/layout/sidebar.tsx` 把 `Badge variant="success|outline"` 替换为 `StatusPill tone="ready|warn"`，移除未用的 Badge import。
- usage/logs/models 页里的 Badge 是数据呈现（如 provider id chip），不属于状态色范畴，按设计文档 §14 不动。

#### Stage 6 — 2026-06-06
- 新建 `apps/web/src/app/api-keys/page.tsx`（Suspense 包裹以支持 `useSearchParams`）+ `components/api-keys/api-keys-client.tsx`：Provider Key 列表 + Save / Test / 清除按钮；Test 调 `POST /api/providers/{id}/test`，结果用 StatusPill 渲染，按 status→tone 映射；`testedAt` 记到 `sessionStorage["modelgate:provider-test-results"]`，刷新仍可见。
- `?provider={id}` deep-link：自动滚动到对应卡并加 primary 边框高亮，配合 Stage 2 ErrorBanner 的 "Edit API key" 链路。
- `components/settings/settings-client.tsx` 精简为单卡片，提示 API Keys 已迁出，留一个跳 `/api-keys` 的按钮；旧 Settings 路由不破坏。

#### Stage 7 — 2026-06-06
- Sidebar footer `Local Mode · v1.0` → `Local Mode · v2.0`。

#### Stage 8 — 2026-06-06
- pytest：`tests/test_provider_test_phase11.py`（9）+ `tests/test_logging_format.py`（8）+ `tests/test_model_registry.py`（3） = 20/20 通过。其他 phase 测试依赖本地 Postgres+Redis，未跑（CLAUDE.md 允许 `require_local_port` 跳过）。
- TypeScript：`npx tsc --noEmit` 主工程 0 错；`tsconfig.e2e.json` + `npx tsc -p tsconfig.e2e.json` 0 错。
- Playwright：`apps/web` 新增 `@playwright/test ^1.60.0` devDep + `playwright.config.ts`（仅 chromium，baseURL `http://localhost:3000`，不自动起 webServer）+ 4 个 spec：`e2e/api-keys.spec.ts`（Test 按钮 happy path，mock 后端）、`e2e/activity.spec.ts`（Activity 表 + /history /logs 重定向）、`e2e/playground.spec.ts`（ModelSelectorRow 渲染 mock 模型）、`e2e/output-tabs.spec.ts`（4 Tabs 可见）。新增 npm 脚本：`e2e` / `e2e:install`。
- 浏览器实测：用户运行 `npm run e2e:install` 下载 chromium（~300MB）+ 起 next dev、uvicorn 后，`npm run e2e` 触发执行。本会话未做浏览器实跑（避免大流量 + 服务起停）。

#### Stage 9 — 2026-06-06
- 收尾本任务三份 tracking docs。设计决策新增 D-2026-06-06-01..04；任务清单清空 V2 P0 项目，列出 P1/P2 后续候选。

---

## 当前任务：修复 Docker build `useSearchParams` Suspense 报错（2026-06-07）

用户反馈 `docker compose up --build` 失败，错误：

```
useSearchParams() should be wrapped in a suspense boundary at page "/activity".
```

根因：V2 P0 改造（2026-06-06）Stage 3 新增 `/activity` 时，`app/activity/page.tsx` 渲染 `<ActivityPage />` 而没包 `<Suspense>`。同一批改造的 `/workspace` 和 `/api-keys` 都用 Suspense 包裹了，漏了 `/activity`。`/activity` 内部的 `useSearchParams` 是深链 `?runId=...` 入口（来自 Output ErrorBanner），不能丢。

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 0 | 扫 `useSearchParams` 全部使用点，确认 `/activity` 是唯一漏的 page | done |
| 1 | `app/activity/page.tsx` 加 `<Suspense>` 包裹 `<ActivityPage />` | done |
| 2 | `rm -rf apps/web/.next` + `npm run build --workspace apps/web` | done |
| 3 | `docker compose build` 全 5 服务验证 | done |
| 4 | 收尾 3 份 tracking 文档 | done |

### Stage log

#### Stage 0 — 2026-06-07
- `grep -rn "useSearchParams" apps/web/src` 命中 3 处：
  - `components/activity/activity-page.tsx:32` — `ActivityPage` 顶层调用，读 `?runId=` 深链。
  - `components/workspace/use-workspace-queries.ts:63` — `useWorkspaceQueries` hook 顶层调用，读 `?taskType=`。
  - `components/api-keys/api-keys-client.tsx:95` — `ApiKeysClient` 顶层调用，读 `?provider=`。
- 全部使用点对应的 page-level 入口：
  - `/activity` → `app/activity/page.tsx` — **无 Suspense**（本次 bug）。
  - `/workspace` → `app/workspace/page.tsx:1-11` — 已有 `<Suspense fallback={...Loading Playground...}>`。
  - `/api-keys` → `app/api-keys/page.tsx:1-11` — 已有 `<Suspense fallback={null}>`。
- 结论：唯一缺 Suspense 的是 `/activity`，修这一个点即可。

#### Stage 1 — 2026-06-07
- `app/activity/page.tsx` 从 7 行扩到 12 行，加 import `Suspense` + JSX 包裹：
  ```tsx
  import { Suspense } from "react";
  import { ActivityPage } from "@/components/activity/activity-page";

  export default function ActivityRoute() {
    return (
      <Suspense fallback={null}>
        <ActivityPage />
      </Suspense>
    );
  }
  ```
- 选择 `fallback={null}` 与 `/api-keys` 一致：Activity 表格内容多，加 skeleton 反而闪；null fallback 让页面在客户端 hydration 完成后才渲染，体验与现状一致。
- **不**做"把 `useSearchParams` 单独抽到子组件"那种最小化拆分：项目已有约定（`/workspace` / `/api-keys` 都是直接包整个客户端组件），保持一致。
- **不**用 `next.config.ts` 的 `experimental.allowedRevalidateHeaderKeys` 或 `dynamic = "force-dynamic"` 关掉静态预渲染：本任务是 build-time 报错，关掉 SSG 是过度反应。
- **不**改 `ActivityPage` 内部：它只是被动调用 `useSearchParams`，不挪动。

#### Stage 2 — 2026-06-07
- `rm -rf apps/web/.next`（按 `[[project-next-cache]]` 记忆清缓存）。
- `npm run build --workspace apps/web`：
  - `✓ Compiled successfully in 8.0s`
  - `✓ Generating static pages (12/12)`
  - `/activity` 8.15 kB，prerendered as static content（与 `/api-keys` / `/workspace` 同样标 ○ Static）。
  - 0 error；只剩 4 条 unused-import warning，都在 `workspace/{mode-tabs,workspace-shell}.tsx` 里，与本次修复无关（CLAUDE.md 禁止 drive-by refactor）。

#### Stage 3 — 2026-06-07
- `docker compose build`（全 5 服务）：
  - `modelgate-postgres` / `modelgate-redis` / `modelgate-api` / `modelgate-worker` / `modelgate-web` 全部 `Built`。
  - web builder 阶段日志与本地 `npm run build` 一致：`prerendered as static content`，无 Suspense 警告。
- 没跑 `docker compose up`：up 会拉起 postgres + redis + api + worker 等待 healthcheck，依赖 `.env` 和本地端口，不在 build 修复范围内。`docker compose build` 已覆盖用户原话"docker compose up --build" 里的 build 验证部分。

#### Stage 4 — 2026-06-07
- 收尾 3 份 tracking docs。
- 设计决策新增 D-2026-06-07-01：`/activity` 改用 `<Suspense fallback={null}>`，与 `/api-keys` 同构。
- 任务清单：本任务入 Done。
- 范围限定：仅 1 个文件 12 行；未触 `app/*`、后端、`tests/*`、依赖、env；按 staged-execution 范围限制不补 pytest（CLAUDE.md "Ship an automated check" 的"any backend change" 不适用）。

### 最终改动文件

```
apps/web/src/app/activity/page.tsx   # +6/-0  包裹 <Suspense>
```

### 已知 / 待跟进
- 无。`docker compose up --build` 在干净环境应能继续（web 服务 build 已确认无报错）；up 阶段需用户配置 `.env` + 5432/6379 端口，按现有 README Quick start 走。

---

## 当前任务：V2 未完成项补设计 + 实施（2026-06-07 起）

V2 现状评估发现 7 处缺口（3 大：Compare / Templates / Preset；4 小：错误码字典 / Rerun 模型选择器 / Export 格式 / Test Connection 探测策略）。本次：
- 把 7 处缺口都补到 `docs/05-前端设计/前端V2界面与功能优化方案.md`。
- 实施 P1 Templates + P2 Preset + P2 Compare。

**范围锁定（用户 2026-06-07 确认）：**
- 模板存储用 localStorage（不动后端 schema）。
- Compare 走前端并行，最多 3 模型。
- 设计文档先全部补完再做实施。

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 0 | 写入任务清单 | done |
| 1 | 补 §16 Compare、§17 Templates、§18 Preset 三大节 | done |
| 2 | 补 §19 错误码字典、§20 Rerun 模型选择器、§21 Export 格式、§22 Test Connection 探测策略 | done |
| 3 | 实施 Prompt 模板（localStorage） | done |
| 4 | 实施参数 Preset | done |
| 5 | 实施 Compare 多模型对比（并行 3 个） | done |
| 6 | 验证：pytest + tsc + web:lint + Playwright | done |
| 7 | 收尾 tracking docs | done |

### Stage log

#### Stage 0 — 2026-06-07
- 读完 5 份上下文（README、CLAUDE、3 份 tracking、V2 方案 778 行）。
- 通过 AskUserQuestion 锁定范围：全部 7 处缺口设计 + 实施 P1 Templates + P2 Preset + P2 Compare。Compare 走并行最多 3 个；模板用 localStorage。
- 任务清单写入"高优先级"区，列出 7 个 stage。

#### Stage 1 — 2026-06-07
- `docs/05-前端设计/前端V2界面与功能优化方案.md` 追加 §16 Compare、§17 Templates、§18 Preset 三大节。
- §16 关键决策：3 个 slot 上限、并行 `Promise.all` 触发现有 `streamChatRun`、`compare_group_id` 用 `runs.metadata` 写入（零 schema 变更）、不支持 generation 任务。
- §17 关键决策：localStorage 键 `modelgate:prompt-templates`、版本 1、内置 13 条模板、损坏自动备份到 `.bak`、Export/Import JSON。
- §18 关键决策：4 个内置 Preset target 是数字字段，schema-aware 自动 clip 到 min/max，不支持的字段 skip 而非报错。

#### Stage 2 — 2026-06-07
- 调用 Explore subagent 扫 provider adapter 现状，拿到 16 个 `AppError.error_type` 字符串 + `_classify_probe_error` 映射 + 3 类 adapter 探测路径。
- §19 错误码字典：16 行映射表（errorType → StatusPill tone → 用户文案 → 推荐操作）+ 落地位置 `lib/errors/dictionary.ts`。
- §20 Rerun 模型选择器：决定走"跳 Playground + `?fromRun=` 深链"而非抽屉内独立选择器（避免双重 ModelSelectorRow）。
- §21 Export/Delete 格式：JSON 单文件 / ZIP 双模、Mask prompt/output/file 默认勾选、API key 绝不导出、Delete 要 `DELETE` 全大写二次确认。
- §22 Test Connection：3 类 adapter 各自探测 endpoint + Volcengine "GET 假 task id → 404 = ok" 特殊分支。

#### Stage 3 — 2026-06-07
- 重写 `apps/web/src/lib/prompt-templates.ts`：从"硬编码 record"扩到完整 store（localStorage 读写、saveUserTemplate、deleteUserTemplate、importUserTemplates、exportUserTemplates、损坏自动备份）；保留原 13 条中文 builtin 模板不变。
- 新建 `components/workspace/prompt-template-popover.tsx`：Builtin / Saved 分组下拉 + Save current 模态 + Manage 模态（导入/导出 JSON、表格化用户模板列表、删除）。
- `workspace-shell.tsx` 删掉内联 `PromptTemplatePopover`、注册新组件；选 template 带 recommendedParams 时插入轻量"Apply params?"确认条。
- TypeScript 报 template-literal 类型推断错（`Map<\`${string}::${string}\`, ...>`），改用显式 `Map<string, PromptTemplate>` 修复。

#### Stage 4 — 2026-06-07
- 新建 `apps/web/src/lib/param-presets.ts`：4 个 builtin preset（Creative / Precise / Long context / Low cost）+ `applyPreset()` 函数处理 schema-aware 映射（字段不存在→skip，非数字→skip，超限→clip）+ Long context 的 max_tokens 80% × 256 取整逻辑。
- `params-popover.tsx` 顶部加 Preset 行：下拉显示对当前 taskId 适用的 preset、切换需二次确认、应用后给"X fields set, Y skipped"反馈、`+ Save current as preset` 灰显（标 "Coming in a later version"）。
- `workspace-shell.tsx` 给 ParamsPopover 加 `taskId` 和 `onApplyMany` props，对接现有 `q.setParams` 批量写入。

#### Stage 5 — 2026-06-07
- 新建 `components/workspace/compare-drawer.tsx`：右抽屉布局（max-w-5xl）、3 slot 上限、最少 2 个才能 Run、共享 Prompt/Params/files、复用 `streamChatRun` 并行 `Promise.all` 触发、每个 slot 独立状态机（idle/running/ok/failed/cancelled）、结果区三列对照（latency + tokens placeholder + 输出文本 + Copy/Open log）。
- `_compare_group_id` 通过 `params` 字段透传给后端（不动 endpoint contract，后端写入 runs.metadata 由现有 jsonb 字段承接）。
- `workspace-shell.tsx` 加 `Compare` 按钮（GitCompare 图标）、disabled 条件：未选模型 / 可用模型 < 2 / prompt 空；打开抽屉时把当前主模型 + 下一个推荐模型预填为 slot A/B。

#### Stage 6 — 2026-06-07
- pytest：`test_provider_test_phase11` + `test_logging_format` + `test_model_registry` + `test_usage_models_phase10` = 28/28 通过（1.17s）。
- `npm run typecheck --workspace apps/web`：主工程 0 错。
- `tsc -p apps/web/tsconfig.e2e.json --noEmit`：e2e 0 错。
- `npm run web:lint`：0 error；3 处 react-hooks/exhaustive-deps 是设计意图（refreshKey 触发 localStorage 重读、compareGroupId 仅在 open 翻转时重生成），加 inline eslint-disable 注释明确意图后剩 1 个 pre-existing warning（`mode-tabs.tsx` unused `cn`），属预存在，按 CLAUDE.md "no drive-by refactor" 不动。
- `rm -rf apps/web/.next && npm run build --workspace apps/web`：12/12 静态预渲染成功，`/workspace` 38kB（含 3 个新组件）。
- 新增 `apps/web/e2e/playground-followups.spec.ts`：2 个 spec 覆盖 Templates 下拉 + Compare 按钮启用条件与抽屉打开（mock providers + models + usage，无需后端）。

#### Stage 7 — 2026-06-07
- 收尾本任务三份 tracking docs。设计决策新增 D-2026-06-07-02..06。任务清单清空"高优先级"区，本任务入 Done。
- 范围限定：本任务**纯前端 + 纯文档**，未触 `apps/server/*`、未触 `tests/*`（CLAUDE.md "Ship pytest for any backend change" 不适用），未触 env / docker-compose / dependencies。

### 最终改动文件

```
docs/05-前端设计/前端V2界面与功能优化方案.md  # +500 行 §16-§22
docs/04-开发管理/{进度跟踪,设计决策,任务清单}.md
apps/web/src/lib/prompt-templates.ts          # 重写
apps/web/src/lib/param-presets.ts             # 新建
apps/web/src/components/workspace/prompt-template-popover.tsx  # 新建
apps/web/src/components/workspace/compare-drawer.tsx           # 新建
apps/web/src/components/workspace/params-popover.tsx           # +Preset 行
apps/web/src/components/workspace/workspace-shell.tsx          # 集成 3 个新组件
apps/web/e2e/playground-followups.spec.ts     # 新建
```

### 已知 / 待跟进

- Compare C3/C4（Per-model params + Metrics 排序行 + Rerun slot + 后端 compare_group_id 落库 + Activity 过滤）按设计文档 §16.10 留作下一 task。
- Preset P3（"Custom · modified from X" 标签 + 字段级 modified 小点）按设计文档 §18.5 留作下一 task。
- §20 / §21 Rerun & Export/Delete 仅有设计，无代码；待用户决定优先级后再开 task。
- pre-existing warning `apps/web/src/components/workspace/mode-tabs.tsx:4 'cn' is defined but never used`，与本任务无关，按"no drive-by refactor"保留。

---

## 当前任务：V2 剩余 7 项实施（2026-06-08 起）

用户选 A+B 组全部（3 纯前端 + 4 含后端），共 8 stage。

**范围锁定（用户 2026-06-08 确认）：**
- Stage 1-3 纯前端可并行；Stage 4 独立后端；Stage 5→6 串行（后端→前端）；Stage 7 内部串行（migration→前端）。

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 1 | §19 错误码字典 + ErrorBanner（纯前端） | done |
| 2 | §20 Rerun with another model（纯前端） | done |
| 3 | Preset P3 — modified 小点 + Custom 标签（纯前端） | done |
| 4 | §22 Test Connection Volcengine 适配（后端） | done |
| 5 | §21 Export/Delete 后端（DELETE + Export endpoint + pytest） | in progress |
| 6 | §21 Export/Delete 前端（Settings 页 + Export/Delete 模态） | done |
| 7 | Compare C3/C4（Alembic migration + Per-model params + Metrics 排序） | done |
| 8 | 收尾 + 验证 + tracking docs | done |

### Stage log

#### Stage 1 — 2026-06-08
- 新建 `apps/web/src/lib/errors/dictionary.ts`：40+ errorType → {tone, message, actions[]} 映射。
- 新建 `apps/web/src/components/workspace/error-banner.tsx`：可复用 ErrorBanner 组件。
- 改 `output-section.tsx`：替换内联 ErrorBanner + `describeError`。
- 改 `compare-drawer.tsx`：失败 slot 用 ErrorBanner。
- 改 `activity-page.tsx`：详情抽屉错误区用字典文案。

#### Stage 2 — 2026-06-08
- 改 `use-workspace-queries.ts`：`?fromRun=` 深链处理，自动预填 taskType/prompt/params。
- 改 `model-selector-row.tsx`：`originalModelId` prop + `Original` 标签。
- 改 `workspace-shell.tsx`：传递 `originalModelId`。
- 改 `activity-page.tsx`：详情抽屉加 "Rerun with another model" 按钮。

#### Stage 3 — 2026-06-08
- 改 `params-group.tsx`：`modifiedFields` prop + 蓝色小点。
- 改 `params-popover.tsx`：`modifiedFields` Set + `userModifiedAfterPreset` + `Custom · modified from X` 标签。

#### Stage 4 — 2026-06-08
- 改 `apps/server/app/api/providers.py`：`test_provider_connection` 增加 `_try_generation_probe()` 分支（Volcengine Seedance GET probe，404=ok）。
- 新建 `tests/test_provider_test_volcengine_phase12.py`：9 条用例全部通过。
- 回归验证：28 条已有测试全通过。

#### Stage 5 — 2026-06-08
- 改 `apps/server/app/api/usage.py`：新增 `DELETE /api/usage/logs/{log_id}`（单条删除，级联 Run/GenerationTask + RequestLogs）、`DELETE /api/usage/logs?olderThan=`（批量删除）、`GET /api/usage/export?scope=&format=&mask=&olderThan=`（JSON/ZIP 双模导出，mask 替换 prompt/output）。
- 新建 `tests/test_usage_delete_export_phase12.py`：12 条用例全通过。
- 回归验证：49 条已有测试全通过。

#### Stage 6 — 2026-06-08
- 新建 `apps/web/src/components/ui/dialog.tsx`：Radix Dialog 封装。
- 新建 `apps/web/src/components/settings/export-modal.tsx`：Scope 选择（usageLogs/runs/requestLogs）、Format（JSON/ZIP）、Mask 开关、下载。
- 新建 `apps/web/src/components/settings/delete-modal.tsx`：Days 输入、`DELETE` 全大写二次确认、结果展示。
- 改 `apps/web/src/components/settings/settings-client.tsx`：新增 Privacy & Data 区，Export Data / Delete Local Data 按钮。
- 验证：tsc 0 错、lint 仅预存 warning、build 成功。

#### Stage 7 — 2026-06-08
- 改 `apps/server/app/db/models.py`：`Run` 模型新增 `metadata_json` 列（JSON, nullable）。
- 新建 `apps/server/alembic/versions/0003_runs_metadata_json.py`：`runs` 表加 `metadata_json jsonb` 列。
- 改 `apps/server/app/services/chat_runtime.py`：`run_chat` / `stream_chat` 接受 `compare_group_id` 参数，写入 `Run.metadata_json`。
- 改 `apps/server/app/api/chat.py`：`CreateRunInput` 新增 `compareGroupId` 字段；`serialize_run` 输出 `metadata`。
- 改 `apps/server/app/api/usage.py`：`GET /api/usage/logs` 新增 `compareGroupId` query param，过滤 `Run.metadata_json->>compare_group_id`。
- 新建 `tests/test_compare_group_phase12.py`：4 条用例全通过。
- 改 `apps/web/src/components/workspace/use-workspace-queries.ts`：`streamChatRun` 接受 `compareGroupId` 顶层参数。
- 改 `apps/web/src/components/workspace/compare-drawer.tsx`：Per-model params（每个 slot 独立 `paramsOverride`）、Metrics 排序（Latency / Tokens 按钮）、`compareGroupId` 正确传递。
- 验证：53 条测试全通过、tsc 0 错、build 成功。

#### Stage 8 — 2026-06-08
- 全量验证：53 条 pytest 全通过、tsc 0 错、lint 仅预存 warning、build 成功。
- 预存失败确认：`test_security_phase8.py::test_frontend_source_does_not_reference_provider_api_keys` 在改动前已失败（`/api-keys` 路由路径包含 `api-key` 子串），非本次回归。
- 收尾 3 份 tracking docs。

---

## 当前任务：V2 低优先级 7 项实施（2026-06-08 续）

用户确认 API Keys 多 key 轮转（P2）推迟到 V3。剩余 7 项全部纯前端改动，零后端变更。

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 1a | `mode-tabs.tsx` 未使用 `cn` import 清理 | done |
| 1b | `/api-keys` deep-link pulse 动画 | done |
| 2a | Activity 页 `?compareGroupId=` 过滤入口 | done |
| 2b | 自定义 Preset 保存（§18.4） | done |
| 2c | 模型管理页 Compare/Diff 视图 | done |
| 2d | Usage 页 Provider × Model 热力图 | done |
| 3 | Playwright E2E 测试 | done |
| 4 | 收尾 + 验证 + tracking docs | done |

### Stage log

#### Stage 1a — 2026-06-08
- 删除 `apps/web/src/components/workspace/mode-tabs.tsx` 中未使用的 `import { cn } from "@/lib/utils"` 行。

#### Stage 1b — 2026-06-08
- 改 `apps/web/src/components/settings/api-keys-client.tsx`：`?provider=xxx` deep-link 命中时，对应 provider 卡片加 CSS pulse 动画（自定义 `@keyframes ring-pulse`，2s 后自动停止）+ border 高亮组合。

#### Stage 2a — 2026-06-08
- 改 `apps/web/src/components/activity/activity-page.tsx`：
  - URL 读取 `?compareGroupId=` 参数
  - 过滤器区新增 "Compare Group" 条件（当 URL 有值时显示 chip，可清除）
  - 传递 `compareGroupId` 给 `getData("/usage/logs", { compareGroupId })` 调用

#### Stage 2b — 2026-06-08
- 改 `apps/web/src/lib/param-presets.ts`：
  - 新增 `saveCustomPreset(name, params)` / `loadCustomPresets()` / `deleteCustomPreset(id)` 函数
  - 存储 key：`modelgate:custom-presets`，schema version = 1
  - 每条：`{ id: crypto.randomUUID(), name, params, createdAt }`
- 改 `apps/web/src/components/workspace/params-popover.tsx`：
  - Preset 下拉列表追加 custom presets（分隔线后）
  - `+ Save current as preset` 按钮取消灰显，点击弹 inline input 确认名称
  - Custom preset 项右侧加删除图标（hover 显示）

#### Stage 2c — 2026-06-08
- 改 `apps/web/src/components/models/model-management.tsx`：
  - PageHeader 加 Compare 模式切换按钮（GitCompare icon）
  - Compare 模式：每行 checkbox 选中 2-3 个模型
  - 选中 ≥2 时底部 sticky bar 显示 "N models selected" + Compare 按钮
  - 点击打开 Dialog 横向并列对比：Provider / Runtime / Category / Input types / Output types / Context window / Async / Capabilities / Status
  - 复用已有 shadcn/ui Dialog 组件

#### Stage 2d — 2026-06-08
- 改 `apps/web/src/components/usage/usage-page.tsx`：
  - UsageHeatmap 函数：CSS Grid 实现（行=Provider，列=Model），cell 颜色深浅映射 usage 次数
  - Provider/Model 名称 title 属性（truncation 时仍可看全名）
  - Cell tooltip：`{provider} × {model}: {count} requests`
  - 空数据时显示 placeholder
  - 数据源：复用 `GET /api/usage/models` 返回的 per-model 数据

#### Stage 3 — 2026-06-08
- 新建 `apps/web/e2e/models-compare.spec.ts`：2 条用例（Compare 模式切换 + 选中 2 模型打开对比 Dialog）
- 新建 `apps/web/e2e/usage-heatmap.spec.ts`：1 条用例（热力图渲染 provider/model 标签 + cell tooltip）
- 新建 `apps/web/e2e/settings-export-delete.spec.ts`：2 条用例（Export 模态打开 + Delete 模态二次确认）
- 5/5 Playwright 测试通过（chromium）
- 踩坑记录：Radix Dialog 渲染 `<div role="dialog">` 非 `<dialog>` 元素；热力图 title 属性需 XPath ancestor scope 防止与表格重复

#### Stage 4 — 2026-06-08
- 全量验证：tsc 0 错、lint 仅预存 warning
- 更新 3 份 tracking docs

---

## 当前任务：V2.5 Project Mode MVP（2026-06-08 起）

按 `docs/04-开发管理/ProjectMode实施计划.md`（已重命名为 V2.5），实施多 Agent 协同任务控制台 MVP。**只生成方案 artifact，不写业务代码**。

**范围锁定（用户 2026-06-08 确认）：**
- 完整 MVP：Intake + Planner + Worker × N + Supervisor + Integrator
- 后端 orchestrator + asyncio.create_task（不引 Celery）
- 5 张新表 + Alembic 0004
- 全套 budget（max_agents + max_rounds + max_tokens + max_runtime + max_context_files）
- Artifact DB 存储（jsonb，≤ 1MB）
- 验收：5 个 phase pytest + 3 个 Playwright spec

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 0 | 重读 5 份上下文 + 任务清单写入 + 重命名 ProjectMode V2.2/2.3/2.4 → V2.5/2.6/2.7 | done |
| 1 | DB schema：5 张新表 + Alembic `0004_project_mode_tables.py` | done |
| 2 | 后端 `project_runtime/` 服务层骨架 + schema validator + budget tracker | done |
| 3 | Planner Agent：system prompt + 输出 schema + artifact 写入 | done |
| 4 | Worker × N 并行执行：asyncio.gather + Semaphore + allowed_files | done |
| 5 | Supervisor + Integrator + final-implementation-plan.md | done |
| 6 | API 路由 `projects.py`：5 个 endpoint + SSE events | done |
| 7 | 前端 `/projects` 列表 + `/projects/[id]` 详情 + Agent Board + Sidebar | done |
| 8 | Playwright 3 个 spec：create / approval / board | done |
| 9 | 收尾 3 份 tracking docs + 设计决策 | done |

### Stage log

#### Stage 0 — 2026-06-08
- 重读 README / CLAUDE / 三份 tracking docs / ProjectMode 实施计划 / 前端 V2 方案。
- 两 Explore agent 并行扫前后端：复用候选确认（chat_runtime / model_registry / Run/UsageLog/RequestLog / CompareDrawer / OutputSection Timeline / StatusPill / Activity 抽屉 / pytest phase11 fixture 三件套）。
- 用户 4 个 AskUserQuestion 锁定：V2.5 命名 / 后端 orchestrator / 完整 MVP / 全套 budget / asyncio / DB-stored artifacts / 分 phase pytest。
- `ProjectMode实施计划.md`：V2.2 → V2.5、V2.3 → V2.6、V2.4 → V2.7 重命名；§10 当前执行建议同步更新。
- 任务清单"高优先级"区写入 V2.5 任务条目。
- 计划文件落盘 `/Users/kepeng/.claude/plans/sleepy-stirring-iverson.md`。

#### Stage 1 — 2026-06-08
- `apps/server/app/db/models.py` 新增 5 个 ORM class：`ProjectRun` / `ProjectTask` / `AgentRun` / `Artifact` / `ProjectMemory`；全部带 `id (str, pr_xxx / t_xxx / ag_xxx / art_xxx / mem_xxx)`、`created_at / updated_at`、`*_json` 字段。
- `AgentRun.run_id` 外键关联现有 `runs.id`，复用 `Run` / `UsageLog` / `RequestLog` 的 Token 计费。
- 新建 `apps/server/alembic/versions/0004_project_mode_tables.py`：`down_revision = "0003_runs_metadata_json"`，5 张新表 + 索引全建；downgrade 全部 drop。

#### Stage 2 — 2026-06-08
- 新建 `apps/server/app/services/project_runtime/` 7 个文件：
  - `__init__.py` 导出 `Budget / BudgetExceeded / BudgetTracker / ProjectOrchestrator / ProjectRuntimeView` + schema 类。
  - `budget.py`：`Budget` dataclass（默认 6/1/200k/600s/8）、`BudgetTracker`（reserve_agent / add_tokens / reserve_round / check_runtime / reserve_context_files / usage_snapshot）、`BudgetExceeded(reason, limit_kind, limit_value, current_value)`。
  - `schemas.py`：5 个 Pydantic 模型 + `validate_agent_output(role, payload)`；`SupervisorOutput` 用 `model_config = ConfigDict(populate_by_name=True)` 把 `pass` 别名映射到 `pass_check`。
  - `prompts.py`：5 段 system prompt（Intake / Planner / Worker / Supervisor / Integrator），全部强制 JSON-only 输出。
  - `artifacts.py`：`MAX_ARTIFACT_BYTES = 1 MiB`；`write_artifact` 自动判 dict/list 走 `content_json` 或 str 走 `content_text`，超限截断 + `truncated=True`；`serialize_artifact` 输出 frontend-friendly dict。
  - `agents.py`：`_run_agent` 异步调用 `chat_runtime.run_chat`、写 `AgentRun` 行、解析 markdown-fenced JSON、validate schema；5 个公共 agent 函数（`run_intake / run_planner / run_worker / run_supervisor / run_integrator`），Worker 走 `schema_role="worker"` 适配 7 个 sub-role；BudgetExceeded 单独捕获并 re-raise（让 orchestrator 终止并行任务）。
  - `orchestrator.py`：`ProjectOrchestrator.run`（Intake → Planner → `awaiting_approval`）+ `run_approved`（Workers 并行 → Supervisor → Integrator → `completed`）+ `pop_events` SSE 队列 + `_run_workers_parallel` 用 `asyncio.Semaphore(WORKER_CONCURRENCY=4)` + 独立 `SessionLocal()` 避免跨协程共享 SQLAlchemy session。
- `tests/test_project_mode_phase13.py` 24 用例：Schema 9 / Budget 13 / Artifact 2 全过。修复 `_now_iso` 改返回 `datetime`（SQLite 不接 ISO 字符串）+ `check_runtime` 用 `>=` 让 max=0 即触发。

#### Stage 3 — 2026-06-08
- `tests/test_project_mode_phase14.py` 9 用例覆盖 run_intake / run_planner：happy path / markdown-fenced / 非法 JSON / schema 违规 / budget exceeded / 计数 + agent 关联 artifact 写入。Mock `chat_runtime.run_chat` 返回可配置 canned JSON，全程不依赖真实 provider。

#### Stage 4 — 2026-06-08
- `tests/test_project_mode_phase15.py` 4 用例：3-task 并行完成 + artifact 持久化 + budget 在并行中触发 BudgetExceeded + Semaphore 并发上限。
- `_run_workers_parallel`：每 worker 独立 SessionLocal（避免 SQLAlchemy session 跨协程）+ `asyncio.Semaphore(4)` + `asyncio.gather(..., return_exceptions=True)` 后再抛 `BudgetExceeded` 给外层 `try/except`。
- 修 `prompts.py::worker_prompt`：把 `WORKER_PROMPT.format(role=...)` 改成 `.replace("{role}", role)`，避免 `{}` JSON 模板触发 KeyError。

#### Stage 5 — 2026-06-08
- `tests/test_project_mode_phase16.py` 4 用例：Intake+Planner 端到端到 `awaiting_approval` + approve 后跑到 `completed` + budget=1 触发 failed + 事件 SSE 队列验证。
- 修 `orchestrator.py`：`_update(status)` 必须在 `_save_and_event` 之前调用（之前顺序反了，导致 status 不进 DB）+ `run_approved` 末尾 `db.commit()` 持久化 `completed`。
- 修 `run_approved` 中 `planner_agent` 查询：`db.query(type(project_run).__class__)` → `db.query(AgentRun)` 正确按 role 取最新。

#### Stage 6 — 2026-06-08
- 新建 `apps/server/app/api/projects.py`：6 个 endpoint，全部走 `{"data": ...}` envelope（与 CLAUDE.md 约定一致）。
  - `POST /api/projects` — 创建 + 后台 `asyncio.create_task(orchestrator.run)`；endpoint 是 `async def`（FastAPI sync 跑在 threadpool，没 event loop 不能 create_task）。
  - `GET /api/projects` — 列表，按 `created_at desc` 限 limit 1..200。
  - `GET /api/projects/{id}` — 详情（projectRun + tasks + agentRuns + artifacts）。
  - `GET /api/projects/{id}/events` — SSE 端点：`pop_events` 排空 + 轮询 status 到终态后发 `closed`。
  - `POST /api/projects/{id}/approve` — 校验 status=`awaiting_approval`、必填 `taskIds`、异步触发 `run_approved`。
  - `POST /api/projects/{id}/cancel` — 校验 status ∈ {running, awaiting_approval} → cancelled。
  - `GET /api/projects/{id}/artifacts/{artifact_id}` — 单 artifact 详情。
- `main.py` 注册 `app.include_router(projects.router, prefix="/api/projects", tags=["projects"])`。
- `tests/test_project_mode_phase17.py` 12 用例覆盖所有 endpoint：create (success + 422) / list / get (200 + 404) / approve (200 + 409 + 404) / full approve flow 到 completed / cancel (200 + 404) / artifact (200 + 404)。测试用 SQLite + monkeypatch 3 件套（startup_module / session_module / provider_secrets_module / orch_module）+ 异步 mock `chat_runtime.run_chat` 按 system_prompt 关键词路由 canned JSON。

#### Stage 7 — 2026-06-08
- `apps/web/src/lib/api.ts` 追加 `projectApi` + 5 个 View 类型（ProjectRunView / ProjectTaskView / AgentRunView / ArtifactView / ProjectRunDetails）。
- 新建 `apps/web/src/app/projects/page.tsx`（列表 + 创建按钮 + 自动轮询 active 状态）+ `apps/web/src/app/projects/[id]/page.tsx`（详情 + Approve / Cancel 按钮 + 子组件编排）。
- 新建 `components/projects/`：5 个文件
  - `project-create-modal.tsx` — Dialog 形态，goal + title + plannerModelId + 3 个 budget 字段。
  - `task-tree-approval.tsx` — Planner 输出任务树勾选 + Approve 按钮。
  - `agent-board.tsx` — Intake / Planner / N × Worker / Supervisor / Integrator 卡片网格（4 列响应式），每个 card 列 artifact 按钮可点开。
  - `budget-meter.tsx` — 3 条 Progress（Agents / Tokens / Runtime）。
  - `artifact-drawer.tsx` — 复用 `ui/sheet` 抽屉，json 自动 pretty-print。
- `components/layout/sidebar.tsx` `navItems` 追加 `{ href: "/projects", label: "Projects", icon: Bot }`。
- 验证：`npm run typecheck` 0 错 + `npm run lint` 0 错 + `npx next build` 12/12 路由 prerender 成功（含新 `/projects` 6.53 kB + 动态 `/projects/[id]` 9.68 kB）。

#### Stage 8 — 2026-06-08
- 新建 3 个 Playwright spec（端到端不依赖后端，全部 mock `/api/projects*`）：
  - `e2e/projects-list.spec.ts` 3 用例：列表渲染 + status pill + 模态创建。
  - `e2e/projects-approval.spec.ts` 2 用例：approve 全选 + 取消勾选后 approve。
  - `e2e/projects-board.spec.ts` 4 用例：agent board 全角色卡片 + budget meter 显示 + artifact drawer 打开 + completed 状态无 cancel 按钮。
- 9/9 Playwright 通过（chromium，~9.5s）。

#### Stage 9 — 2026-06-08
- 全量验证：53/53 phase13-17 pytest 通过 + 9/9 Playwright 通过 + tsc 0 错 + lint 0 错 + next build 12 路由成功。
- 收尾三份 tracking docs（本条进度跟踪 + 设计决策 D-2026-06-08-15..18 + 任务清单 V2.5 入 Done）。

### 最终改动文件

```
# 后端
apps/server/app/db/models.py                              # +5 ORM class
apps/server/alembic/versions/0004_project_mode_tables.py  # 5 张新表 migration
apps/server/app/services/project_runtime/__init__.py       # 导出
apps/server/app/services/project_runtime/budget.py
apps/server/app/services/project_runtime/schemas.py
apps/server/app/services/project_runtime/prompts.py
apps/server/app/services/project_runtime/artifacts.py
apps/server/app/services/project_runtime/agents.py
apps/server/app/services/project_runtime/orchestrator.py
apps/server/app/api/projects.py                            # 6 个 endpoint
apps/server/app/main.py                                    # 注册 router

# 测试
tests/test_project_mode_phase13.py    # 24 用例
tests/test_project_mode_phase14.py    #  9 用例
tests/test_project_mode_phase15.py    #  4 用例
tests/test_project_mode_phase16.py    #  4 用例
tests/test_project_mode_phase17.py    # 12 用例

# 前端
apps/web/src/lib/api.ts                                       # +projectApi + types
apps/web/src/app/projects/page.tsx                            # 列表
apps/web/src/app/projects/[id]/page.tsx                       # 详情
apps/web/src/components/projects/project-create-modal.tsx
apps/web/src/components/projects/task-tree-approval.tsx
apps/web/src/components/projects/agent-board.tsx
apps/web/src/components/projects/budget-meter.tsx
apps/web/src/components/projects/artifact-drawer.tsx
apps/web/src/components/layout/sidebar.tsx                   # +Projects 入口
apps/web/e2e/projects-list.spec.ts
apps/web/e2e/projects-approval.spec.ts
apps/web/e2e/projects-board.spec.ts

# 文档
docs/04-开发管理/{进度跟踪,设计决策,任务清单}.md
docs/04-开发管理/ProjectMode实施计划.md                       # V2.x 重命名
```

### 已知 / 待跟进
- V2.6 Patch Mode（统一 diff 生成）和 V2.7 Controlled Auto（自动 apply + 跑测试 + Verifier）按 ProjectMode 实施计划留作下一 task。
- Memory 跨 project_run 复用：表已建好，MVP 只在 run 内读写；复用由 V2.7 引入。
- Celery 队列：MVP 用 asyncio 够用，V2.7 Controlled Auto 时再引。
- `Service` 跨进程 / 多用户：MVP 单用户本地版，与 ModelGate 整体一致。

---

## 当前任务：V2.6 Patch Mode（2026-06-08 起）

按 `docs/04-开发管理/ProjectMode实施计划.md` V2.6，把 Worker Agent 从纯方案生成升级为 unified diff 输出。

**范围锁定（用户 2026-06-08 确认）：**
- Worker 输出 unified diff（`proposed_changes[].patch` + `patch_combined`）
- Orchestrator 读文件内容注入 Worker prompt + validate_patch + 写 patch artifact
- API：apply（直接写项目源码）/ reject / regenerate
- 前端：UnifiedDiffView 组件 + PatchActions + artifact-drawer 集成 + create-modal mode 选择 + per-file approval
- 测试：22 pytest + 5 Playwright

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 0 | 重读上下文 + 更新 tracking docs | done |
| 1 | 后端 Schema + Prompts | done |
| 2 | 后端 Worker Agent 读文件 + 生成 diff | done |
| 3 | 后端 Orchestrator 集成 | done |
| 4 | 后端 API endpoints | done |
| 5 | 前端 UnifiedDiffView + PatchActions | done |
| 6 | 前端集成 | done |
| 7 | 测试 | done |
| 8 | 收尾 tracking docs | done |

### Stage log

#### Stage 0 — 2026-06-08
- 重读 README / CLAUDE / 三份 tracking docs / ProjectMode 实施计划。
- 任务清单"高优先级"区写入 V2.6 任务条目。
- 进度跟踪新建 V2.6 section。
- 计划文件落盘 `/Users/kepeng/.claude/plans/dynamic-noodling-dawn.md`。

#### Stage 1 — 2026-06-08
- `schemas.py`：`WorkerProposedChange` 新增 `patch: str = ""`，`WorkerOutput` 新增 `patch_combined: str = ""`，新增 `PatchValidationResult` BaseModel。
- `prompts.py`：重写 `WORKER_PROMPT`，删除 "You do NOT write actual code"，新增 unified diff 格式指令 + `patch` / `patch_combined` 字段要求。

#### Stage 2 — 2026-06-08
- `agents.py`：新增 `_read_task_file_contents(task, project_root)` 读文件内容（50KB/文件、200KB 总量、50 glob 上限、跳过二进制）；`_worker_user_prompt` 接受 `file_contents` 追加到 prompt；`run_worker` 新增 `project_root` 参数。

#### Stage 3 — 2026-06-08
- `orchestrator.py`：新增 `PROJECT_ROOT`、`HIGH_RISK_PATTERNS`（13 条）、`HIGH_RISK_REASONS` 常量；新增 `_check_high_risk(filepath)` fnmatch 匹配（处理 `**` 限制）；新增 `validate_patch(diff_text, allowed_files)` 解析 diff header 校验越界文件；修改 `_run_workers_parallel._one` 在 patch 模式下读文件 → validate → 写 patch artifact。

#### Stage 4 — 2026-06-08
- `projects.py`：新增 `POST /{id}/patches/{artifact_id}/apply`（git apply --check → git apply → git add）、`POST /{id}/patches/{artifact_id}/reject`（标记 metadata rejected）、`POST /{id}/patches/regenerate`（重跑指定 worker）；修改 `approve` 接受 `fileApprovals` 存入 task metadata。

#### Stage 5 — 2026-06-08
- 新建 `unified-diff-view.tsx`（~160 行）：解析 unified diff → files → hunks → lines，文件头 expand/collapse + 高风险 badge + 行号 + 绿色+/红色-/中性 context。
- 新建 `patch-actions.tsx`（~110 行）：Apply (green) / Reject (red) / Regenerate (blue) 三按钮 + 高风险确认 checkbox + Applied/Rejected 状态。

#### Stage 6 — 2026-06-08
- `artifact-drawer.tsx`：`artifact.type === "patch"` 时渲染 UnifiedDiffView + PatchActions。
- `task-tree-approval.tsx`：patch 模式下展开显示 per-file checkbox 列表，高风险文件默认取消选中，`onApprove` 回调增加 `fileApprovals`。
- `project-create-modal.tsx`：新增 mode Select（advisory / patch / apply_with_approval）。
- `api.ts`：新增 `applyPatch` / `rejectPatch` / `regeneratePatches` + `PatchApplyResponse` / `PatchRejectResponse` 类型 + `validation_failed` status。
- `[id]/page.tsx`：接入 3 个新 mutation + 传递 fileApprovals + validation_failed 状态处理。
- `agent-board.tsx`：worker card 同时展示 type="worker" 和 type="patch" artifacts。

#### Stage 7 — 2026-06-08
- `tests/test_project_patch_mode_phase18.py`：30 条用例（WorkerSchema 5 / PatchValidationResult 2 / ValidatePatch 8 / ApplyPatch 5 / RejectPatch 2 / FileApprovals 2 / Regenerate 3 / CreateWithMode 2 + 原有 1 条），全部通过。
- `apps/web/e2e/projects-patch-mode.spec.ts`：5 条 Playwright 用例（create modal mode selector / patch diff rendering / high-risk badge / apply+reject+regenerate buttons / per-file approval）。
- 回归验证：30/30 phase18 + 111/111 已有 pytest 通过 + tsc 0 错 + lint 仅预存 warning + build 成功。

#### Stage 8 — 2026-06-09
- 收尾三份 tracking docs。

### 最终改动文件

```
# 后端（修改）
apps/server/app/services/project_runtime/schemas.py       # +WorkerProposedChange.patch / WorkerOutput.patch_combined / PatchValidationResult
apps/server/app/services/project_runtime/prompts.py       # 重写 WORKER_PROMPT
apps/server/app/services/project_runtime/agents.py        # +file reading / project_root param
apps/server/app/services/project_runtime/orchestrator.py  # +validate_patch / HIGH_RISK / patch artifact
apps/server/app/api/projects.py                           # +apply/reject/regenerate endpoints + fileApprovals

# 后端（新建）
tests/test_project_patch_mode_phase18.py                  # 30 用例

# 前端（新建）
apps/web/src/components/projects/unified-diff-view.tsx
apps/web/src/components/projects/patch-actions.tsx
apps/web/e2e/projects-patch-mode.spec.ts                  # 5 用例

# 前端（修改）
apps/web/src/components/projects/artifact-drawer.tsx       # +UnifiedDiffView / PatchActions
apps/web/src/components/projects/task-tree-approval.tsx    # +per-file approval
apps/web/src/components/projects/project-create-modal.tsx  # +mode selector
apps/web/src/components/projects/agent-board.tsx           # +patch artifacts on worker card
apps/web/src/lib/api.ts                                    # +patch API types/functions
apps/web/src/app/projects/[id]/page.tsx                    # +patch mutations

# 文档
docs/04-开发管理/{进度跟踪,设计决策,任务清单}.md
```

### 已知 / 待跟进
- V2.7 Controlled Auto（自动 apply patch + 跑 pytest + Verifier Agent）按 ProjectMode 实施计划留作下一 task。
- `git apply` 写入项目源码是直接操作，未来可考虑先写到 staging 目录让用户 preview。

---

## 当前任务：Per-Agent Model Selection（2026-06-09 起）

Project Mode 创建模态从单一 Planner model 输入改为 5 个 Agent 角色独立选模型。后端 orchestrator 之前只读 `planner_model_id`，其余 3 个字段被忽略。

**范围锁定（用户 2026-06-09 确认）：**
- 5 个 agent（Intake / Planner / Worker / Supervisor / Integrator）各有独立模型选择
- 可选模型来自已配置 API key 的 provider 下的模型
- 推荐模型标注 `(Recommended)`
- 后端 orchestrator 按 agent 角色读取对应 model_id，fallback 链：agent-specific → planner → model_fallback

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 1 | 后端：DB 加 `intake_model_id` + orchestrator per-agent model 路由 | done |
| 2 | 前端：create modal 5 个 agent model selector + recommend 标注 | done |
| 3 | 验证：tsc + build + pytest + E2E 测试更新 | done |

### Stage log

#### Stage 1 — 2026-06-09
- `apps/server/app/db/models.py`：`ProjectRun` 新增 `intake_model_id` 字段
- `apps/server/alembic/versions/0005_intake_model_id.py`：新 migration
- `apps/server/app/services/project_runtime/orchestrator.py`：
  - `_execute`：`intake_model = project_run.intake_model_id or planner_model or fallback`
  - `run_approved`：`worker_model` / `supervisor_model` / `integrator_model` 各自 fallback
- `apps/server/app/api/projects.py`：读取 + 序列化 `intakeModelId`

#### Stage 2 — 2026-06-09
- `apps/web/src/lib/api.ts`：`CreateProjectRunBody` + `ProjectRunView` 新增 `intakeModelId`
- `apps/web/src/components/projects/project-create-modal.tsx`：完全重写
  - 5 行 agent model selector（Intake / Planner / Worker / Supervisor / Integrator）
  - `findRecommendedModel`：优先 chat 模型 + contextWindow >= 32768
  - 按 provider 分组下拉，recommended 模型绿色 `(Recommended)` 标注
  - 自动设置所有 agent 为推荐模型

#### Stage 3 — 2026-06-09
- `rm -rf apps/web/.next && npm run build`：13/13 路由成功
- `pytest tests/test_project_patch_mode_phase18.py`：新增 `test_create_with_intake_model_id`，31/31 通过
- E2E 测试更新：`projects-list.spec.ts` + `projects-patch-mode.spec.ts` 补 `intakeModelId` 字段 + 等待模型加载

### 最终改动文件

```
# 后端
apps/server/app/db/models.py                              # +intake_model_id
apps/server/alembic/versions/0005_intake_model_id.py      # migration
apps/server/app/services/project_runtime/orchestrator.py   # per-agent model routing
apps/server/app/api/projects.py                           # +intakeModelId 序列化

# 前端
apps/web/src/lib/api.ts                                   # +intakeModelId 类型
apps/web/src/components/projects/project-create-modal.tsx  # 5 agent model selectors

# 测试
tests/test_project_patch_mode_phase18.py                  # +test_create_with_intake_model_id
apps/web/e2e/projects-list.spec.ts                        # 更新 mock 数据
apps/web/e2e/projects-patch-mode.spec.ts                  # 更新 mock 数据
```

### 已知 / 待跟进
- 无。5 个 agent 角色均可独立选模型，fallback 链完整，前端推荐逻辑就位。

---

## 当前任务：修复 Delete project run 在 Postgres 上点击无反应（2026-06-09）

用户反馈：`/projects/[id]` 详情页和 `/projects` 列表页的 "Delete this project run?" 弹窗里点 Delete 没反应。

**根因：**
1. 后端 `delete_project_run`（`apps/server/app/api/projects.py:304`）对 `project_tasks` 做 bulk delete，但 `parent_task_id` 是自引用 FK 且 alembic 0004 没有 `ondelete="CASCADE"`，含子任务的 run 在 Postgres 上返回 500。
2. 列表页 `deleteMut` 缺 `onError`，错误被吞掉，UI 完全无反馈，看起来"按了没反应"。

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 1 | 写 pytest 复现（SQLite + `PRAGMA foreign_keys=ON`，3 个用例） | done |
| 2 | 后端按 FK 顺序删除 + 先 NULL `parent_task_id` | done |
| 3 | 前端补 `onError` + inline 错误展示 + 清理无意义 stopPropagation | done |
| 4 | 更新 3 份 tracking doc | done |

### Stage log

#### Stage 1 — 2026-06-09
- 新增 `tests/test_project_delete_phase19.py`：3 个用例（subtasks / agent_runs+artifacts+memory / 404）。
- 用 `event.listens_for(engine, "connect")` 注入 `PRAGMA foreign_keys=ON`，让 SQLite FK 行为对齐 Postgres。
- 首次运行：`test_delete_with_agent_runs_and_artifacts_succeeds` FAILED with `IntegrityError: FOREIGN KEY constraint failed [DELETE FROM project_tasks WHERE project_tasks.project_run_id = ?]`，bug 复现成功。

#### Stage 2 — 2026-06-09
- `apps/server/app/api/projects.py::delete_project_run` 改写：
  - 删除顺序：`artifacts` → `agent_runs` → `project_memory` → UPDATE `project_tasks.parent_task_id = NULL` → DELETE `project_tasks` → `db.delete(pr)`。
  - 所有 bulk 操作加 `synchronize_session=False`（避免无谓的 session 同步）。
- 复测：3/3 phase19 通过；回归 `phase17 + phase18 + phase19 = 51/51` 全过。

#### Stage 3 — 2026-06-09
- `apps/web/src/app/projects/page.tsx`：
  - 新增 state `listDeleteError`；`deleteMut` 加 `onError` + `onSuccess` 清空。
  - Dialog 内 `DialogDescription` 下方加 `<p className="text-sm text-destructive" data-testid="list-delete-error">{listDeleteError}</p>`。
  - Cancel 按钮、`onOpenChange`、行内 Delete 触发按钮都在切换时清空错误。
  - 移除 confirm 按钮上无意义的 `onPointerDown` stopPropagation 和 `onClick` 里的 `preventDefault`/`stopPropagation`。
- `apps/web/src/app/projects/[id]/page.tsx`：同样移除 confirm 按钮上无意义的事件吞食；现有 `setDeleteError` 路径 + `onOpenChange` 清空逻辑已正确，保留。
- 验证：`npm run typecheck` 0 错；按 memory 提示 `rm -rf apps/web/.next` 清缓存。

#### Stage 4 — 2026-06-09
- 本节即 Stage 4 产出。同步追加 `设计决策.md` D-2026-06-09-04..06 + `任务清单.md` Done 区。

### 最终改动文件

```
# 后端
apps/server/app/api/projects.py                           # delete_project_run 改顺序

# 前端
apps/web/src/app/projects/page.tsx                        # listDeleteError + onError + 渲染
apps/web/src/app/projects/[id]/page.tsx                   # 清理无意义 stopPropagation

# 测试
tests/test_project_delete_phase19.py                      # 新增，3 个用例

# 文档
docs/04-开发管理/进度跟踪.md                                # 本节
docs/04-开发管理/设计决策.md                                # D-2026-06-09-04..06
docs/04-开发管理/任务清单.md                                # Done 追加
```

### 已知 / 待跟进
- 任务清单已加一条低优先级 TODO：未来若做大版本 DB 改造，可考虑给 `project_tasks` 全部 FK 加 `ondelete="CASCADE"`（新 migration），从 schema 层根治；当前 ORM ordered delete 已经稳定。
- 验证手段都是自动化：`pytest tests/test_project_delete_phase19.py` + 回归 phase17/18 + `npm run typecheck`。无需用户手动 curl/启 dev server。

---

## 当前任务：Project Mode JSON 解析根治 + Agent Board 分层重设计 + V2.7 Controlled Auto（2026-06-09 起）

用户反馈两个问题：(1) Project Mode 触发 agent 后频繁报 `ValueError: Agent output is not valid JSON`；(2) 当前 Agent Board（4 列响应式 grid）看不清工作流分工，希望按 phase 分层 + 红黄绿灯。借此机会把 ProjectMode 实施计划 §V2.7 Controlled Auto（自动 apply patch → 跑 pytest → Verifier 审查）一并落地。

**根因（已定位，不是模型没输出）：** `apps/server/app/services/project_runtime/agents.py:124` 把 role-specific system prompt 通过 `params={"system_prompt": ...}` 传给 `chat_runtime.run_chat`，但 `chat_runtime._build_messages` 完全无视该字段，强制用 `_system_prompt(task_type)` 返回的通用 chat 人格覆盖。"必须输出纯 JSON" 的指令只在 user prompt 末尾出现，弱模型大概率回 prose 介绍 + ```json 围栏 + 后置 prose，现有 `_try_parse_json` 只剥首尾 ``` 围栏对此格式无能为力。

**范围锁定（用户 2026-06-09 确认）：**
- 全套 Stage 1-7（不分批）
- JSON 修复：根治（chat_runtime 接 system_prompt override）+ 后处理（_try_parse_json 增强 + 失败时落 raw_output）双管齐下
- Agent Board 连接线：ReactFlow 库

### Stage 计划

| Stage | 内容 | 状态 |
|---|---|---|
| 0 | 三份 tracking docs 写入新任务 section | done |
| 1 | JSON 解析根治（chat_runtime + agents）+ 后处理 + phase20 pytest | done |
| 2 | Agent Board 分层结构图（ReactFlow） | done |
| 3 | V2.7-A Verifier Agent + pytest_runner + phase21 pytest | done |
| 4 | V2.7-B auto-apply 循环 + phase22 pytest | done |
| 5 | V2.7-C 停止条件 + DB stop_reason + Alembic 0006 | done |
| 6 | V2.7 前端配套（Verifier 节点 / round-counter / stop-banner / test-results-view） | done |
| 7 | 全量验证 + 收尾 tracking docs | done |

### Stage log

#### Stage 0 — 2026-06-09
- 重读 README / CLAUDE / 三份 tracking docs / ProjectMode 实施计划。
- 调查根因：`grep` 确认 `chat_runtime._build_messages` 不接 `system_prompt` 参数（`chat_runtime.py:389-391`），role-specific prompt 被静默丢弃。
- 通过 AskUserQuestion 锁定范围 + 修复策略 + ReactFlow 选型。
- 计划落盘 `/Users/kepeng/.claude/plans/cuddly-nibbling-iverson.md`。
- 三份 tracking docs（进度跟踪 / 设计决策 / 任务清单）同步追加本任务条目。

#### Stage 1 — 2026-06-09
- **根治：** `apps/server/app/services/chat_runtime.py`
  - `run_chat` + `stream_chat` 顶层签名新增 `system_prompt: str | None = None`
  - `_build_messages` 新增 `system_prompt_override: str | None = None`；非空时直接覆盖 `_system_prompt(task_type)` 返回值
  - 不动 `_system_prompt()` 函数本身，保留对其他调用方的默认行为
- **后处理：** `apps/server/app/services/project_runtime/agents.py`
  - `_run_agent` 调用 `chat_runtime.run_chat(..., system_prompt=system_prompt)`，不再走 `params["system_prompt"]` 通道
  - `_try_parse_json` 增强：先剥围栏 → `json.loads` → 失败再用新函数 `_extract_first_json_object`（顶层平衡括号扫描 + 字符串/转义状态机）抽取首个 `{...}` 块 → 仍失败 raise 含前 500 字符 raw preview 的 ValueError
  - `_run_agent` parse 失败专门走新分支：写 `agent_run.status="failed"`、`error_type="JSON_PARSE_ERROR"`、`output_json={"raw": raw[:5000], "parse_error": ...}`、token 计数 / latency 仍记账，方便前端 Drawer 看到模型真实回复
- **测试：** `tests/test_agent_json_parse_phase20.py` 新建，14 用例覆盖：纯 JSON / 围栏 / prose 前后包裹 / 转义引号 / 字符串内嵌花括号 / 完全非 JSON 报错带 preview / `_extract_first_json_object` 4 个边界 / `_run_agent` 落 raw_output / system_prompt 透传为顶层 kwarg 且不再出现在 params 中。14/14 通过。
- **回归：** 修 `tests/test_project_mode_phase16.py` + `phase17.py` 里 `fake_run_chat` mock，改读 `kwargs["system_prompt"]` 而非 `params["system_prompt"]`（之前的 mock 路由依赖旧通道，不改会全部走 fallback 角色路径导致 task 数对不上）。phase13-20 全量回归 **106/106 通过**。

#### Stage 2 — 2026-06-09
- **新依赖：** `@xyflow/react@^12.11.0`（React 19 兼容的 ReactFlow 新包名；原 `reactflow@11` 还在 React 18 peerDeps 上）。
- **重写：** `apps/web/src/components/projects/agent-board.tsx` 整个组件，从 4 列响应式 grid 改为 ReactFlow 5 行分层 DAG：
  - 节点：5 行（Intake / Planner / Workers×N / Supervisor / Integrator），worker 行根据 task 数动态撑开，y 坐标等距（240px）
  - 边：Intake→Planner / Planner→每个 Worker / 每个 Worker→Supervisor / Supervisor→Integrator
  - 自定义 `AgentNode`：复用原 `AgentCard` 视觉，加 `Handle`（target=top / source=bottom），红黄绿灯通过 2px border 颜色表达（emerald-400 / amber-400 / rose-400 / muted）
  - `nodeTypes={agent: AgentNode}`、`fitView` 自动缩放、禁用拖拽（`nodesDraggable={false}`）、保留 `Controls` 提供 zoom in/out/fit
  - side artifacts（progress / decisions）从 DAG 里挪出到下方的扁平 grid，因为它们不属于某个 agent
  - 高度 1200px 固定，避免 ReactFlow 容器在 grid / flex 布局里坍塌
- **保留：** 全部原有 `data-testid` 选择器（`agent-card-intake` / `agent-card-worker-{id}` 等），现有 Playwright spec 不需要改。
- **新测试：** `apps/web/e2e/projects-board.spec.ts` 追加 1 条用例验证 ReactFlow 渲染：`.react-flow__viewport` 存在 + 6 张卡可见 + edges ≥ 5 + 完成态 intake/integrator 节点带 `border-emerald-400` class。
- **验证：** `npm run typecheck` 0 错；`npx tsc -p tsconfig.e2e.json --noEmit` 0 错；`rm -rf .next && npm run build` 全路由（含 `/projects/[id]` 69.9 kB）编译成功，ReactFlow 客户端组件 SSR 兼容（`/projects/[id]` 标 ƒ Dynamic，未尝试预渲染）。

#### Stage 3 — 2026-06-09
- **Verifier schema：** `apps/server/app/services/project_runtime/schemas.py` 新增 `VerifierOutput`（`verdict: pass|fail`、`failed_tests: [FailedTest]`、`analysis: str`、`next_actions: [VerifierNextAction]}`）+ 注册到 `_SCHEMA_BY_ROLE["verifier"]`。
- **Verifier prompt：** `prompts.py` 新增 `VERIFIER_PROMPT`，明确"verdict=pass 仅当 failed_tests 为空且 analysis 确认满足原始 task acceptance criteria"、"next_actions 非空当 verdict=fail 且每条映射到已存在的 worker_role"、"traceback_excerpt 限 3 行节省 token"。
- **Verifier agent：** `apps/server/app/services/project_runtime/agents.py` 新增 `_verifier_user_prompt(...)` + `run_verifier(...)`；复用 `_run_agent` + 新 `system_prompt` 通道（Stage 1 修复），自动应用 schema 验证。
- **pytest_runner：** 新建 `apps/server/app/services/project_runtime/pytest_runner.py`
  - `subprocess.run([sys.executable, "-m", "pytest", ..., "--json-report", "--json-report-file=<rel>"], cwd=project_root, timeout=300)`，不在 in-process 调 `pytest.main()` 避免阻塞 asyncio loop
  - 报告路径用**相对文件名**（`pytest-json-report==1.5.0` 写绝对路径不可靠，相对路径稳定）；落盘后 unlink 清理
  - 必须加 `--json-report` 标志：缺它时若测试 fail，json-report 不会写文件（实测验证）
  - `_coerce_test_paths` 沙箱化：任何含 `..` 或以 `/` 开头的路径静默丢弃
  - 退出码 0=pass、1=fail、2/3/4/5=errors（兜底解析失败时回退）
  - `_ensure_pytest_jsonreport_available` 早返 `result.error` 让 orchestrator 走 verifier fail 而非 crash
- **测试：** `tests/test_project_verifier_phase21.py` 13 用例：4 schema + 2 prompt 渲染 + 2 run_verifier 端到端（happy pass + fail with next_actions）+ 5 pytest_runner（real subprocess，跑 passing/failing/escape-attr/missing-root/timeout）。**13/13 通过**。
- **环境：** `/Users/kepeng/miniforge3/envs/modelgate/bin/pip install pytest-json-report`（venv 缺这个插件，否则 runner 直接返 `error`）。
- **回归：** phase13-21 全量 **119/119 通过**。

#### Stage 4 — 2026-06-09
- **orchestrator `_run_verifier_loop`：** 新增 verifier 循环方法（608 行起）：
  - 每轮收集 patch artifacts → `git apply --check` + `git apply` + `git add` → run_pytest → run_verifier
  - verdict=pass → return；verdict=fail → 按 `next_actions[].worker_role` 重跑 Worker（注入 verifier feedback 到 user prompt）
  - `_apply_patch_artifact` 写临时文件 → apply → 成功后标记 `metadata.applied=True`
  - `_collect_test_paths` 从 task acceptance_criteria 收集测试路径
- **run_worker feedback_prefix：** `run_worker` + `_run_workers_parallel` 新增 `feedback_prefix` 参数，prepend 到 Worker prompt 前面
- **run_approved 集成：** 当 `mode ∈ {controlled_auto, patch, apply_with_approval}` 时调 `_run_verifier_loop`，结果写入 `project_run.stop_reason / stop_round / round`
- **测试：** `tests/test_project_auto_loop_phase22.py` 初始 6 用例
- **修复：** phase22 测试 hang 因 `monkeypatch.setattr(pr_module, "run_pytest", ...)` 未命中 orchestrator 的直接 import → 改为 `monkeypatch.setattr(orch_module, "run_pytest", ...)`；两轮/耗尽测试因 `_run_workers_parallel` 自己 new SessionLocal() 捞不到 SQLite in-memory 数据 → monkeypatch mock `_run_workers_parallel` 为 no-op
- **修复：** orchestrator line 829 `t.role` NameError 因 `t` 不在作用域 → 改为 `if action.get("worker_role") in roles_to_rerun`
- 6/6 phase22 通过；phase13-22 全量回归 **125/125 通过**。

#### Stage 5 — 2026-06-09
- **Budget 新字段：** `max_schema_failures: int = 2`、`max_same_test_failures: int = 2`（含 `from_dict`/`to_dict` 序列化）
- **BudgetTracker 新能力：**
  - `schema_failures_per_task: dict[str, int]` + `record_schema_failure(task_id)` 返回是否 >= max_schema_failures
  - `failed_test_nodeids: list[set[str]]` + `record_failed_tests(nodeids)` 检查相同 nodeid 集合连续 max_same_test_failures 次
- **REPEATED_TEST_FAILURE stop：** `_run_verifier_loop` 在 pytest 后/verifier 前检查即可；命中时返回 stop_reason="REPEATED_TEST_FAILURE"
- **API 透出：** `_serialize_project_run` 新增 `round` / `stopReason` / `stopRound` 字段
- **测试：** phase22 追加 5 用例（重复失败停止 / schema 失败 tracker / 重复测试 tracker / 空 set 忽略 / 序列化字段）
- phase13-22 全量回归 **130/130 通过**。

#### Stage 6 — 2026-06-09
- **新组件：**
  - `round-counter.tsx`：显示 `Round N / max`
  - `stop-reason-banner.tsx`：8 种 stop_reason 映射（VERIFIER_PASS / MAX_ROUNDS / NO_PATCHES / PATCH_APPLY_FAILED / NO_NEXT_ACTIONS / REPEATED_TEST_FAILURE / TOKEN_BUDGET / WORKER_SCHEMA_FAILURES）+ 对应 icon/tone
  - `test-results-view.tsx`：verifier report 折叠面板展示 verdict / analysis / failed_tests（可展开 traceback）/ applied_files / pytest_summary
- **更新组件：**
  - `agent-board.tsx`：新增 `mode` / `round` / `stopReason` / `stopRound` / `maxRounds` props；controlled_auto 模式下在 Integrator 下方（y=1200）显示 Verifier 节点 + RoundCounter + StopReasonBanner
  - `artifact-drawer.tsx`：`type === "verifier_report"` 时渲染 TestResultsView
  - `project-create-modal.tsx`：mode 下拉新增 `controlled_auto` 选项
- **API 类型：** `ProjectRunView` 新增 `round` / `stopReason` / `stopRound` 字段
- **page.tsx：** 向 AgentBoard 透传 mode / round / stopReason / stopRound / maxRounds
- **验证：** `npx tsc --noEmit` 0 错

#### Stage 7 — 2026-06-09
- **全量回归：** phase13-22 全部 130/130 通过（6.16s）
- **TypeScript：** `npx tsc --noEmit` 0 错
- **清理：** stale `mg-pytest-report-*.json` 文件清除
- **收尾：** 三份 tracking docs（进度跟踪 / 设计决策 / 任务清单）更新
- **最终改动文件汇总：** 见下方
