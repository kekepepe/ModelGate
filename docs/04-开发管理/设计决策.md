# 设计决策

每个 task 跑的时候维护一份。条目按时间顺序追加，编号格式 `D-YYYY-MM-DD-NN`。

---

## 上一任务 D-2026-06-04-01..05 — `/usage/models` 重构

（保留作历史参考，不重复。新读者直接看 git log。）

---

## 当前任务 D-2026-06-04-06.. — `docs/` 整理 + `.gitignore` 补 IP 保护

### D-2026-06-04-06 — 散落的设计/项目 md 全部归入 `docs/`

**决策：** 把 4 份前端设计 md + 1 份 `项目总TODO.md` 全部 git mv 到 `docs/` 下相应子目录。命名规范化：去掉 `ModelGate_` 前缀、统一中文、不混英文小写。

**Why:**
- 仓库根目录应当只放工程入口（README、CLAUDE、`.gitignore`、`package.json`、`docker-compose.yml` 等），不应该混设计文档。
- `docs/README.md` 现有的 `00-04-` 编号是设计好的分类，新增 `05-前端设计/` 沿用同一风格。
- 设计文档的命名规范统一成"中文标题 + 文档/设计/规范/清单"后缀，与现有 `00-04-` 子目录里其他文档的命名一致。
- 移动用 `git mv`（对 tracked 文件），保留 git rename 检测能力，不破坏 blame。

**目标映射：**

| 旧路径 | 新路径 |
|---|---|
| `ModelGate_前端视觉参考改造计划.md` | `docs/05-前端设计/前端视觉参考改造计划.md` |
| `ModelGate_前端页面设计.md` | `docs/05-前端设计/前端页面设计.md` |
| `前端usage页面设计.md` | `docs/05-前端设计/前端Usage页面设计.md` |
| `前端配色设计.md` | `docs/05-前端设计/前端配色设计.md` |
| `项目总TODO.md` | `docs/04-开发管理/项目总TODO.md` |

---

### D-2026-06-04-07 — 工作流 tracking 文件改中文命名、归入 `docs/04-开发管理/`

**决策：** `PROGRESS.md` → `进度跟踪.md`、`DECISIONS.md` → `设计决策.md`、`TODO.md` → `任务清单.md`，都搬到 `docs/04-开发管理/`。

**Why:**
- 现有 `00-04-` 编号子目录里 `04-开发管理/` 的主题最贴近"项目执行进度、决策、待办"。
- 中文命名和现有 `04-开发管理/开发规范文档.md`、`04-开发管理/测试策略文档.md` 风格一致；混用英文名（PROGRESS / DECISIONS / TODO）虽然在软件工程里是事实标准，但和这个仓库的 docs/ 命名风格不统一，按用户偏好统一。
- 这 3 份文件之前没 `git add` 过，是 untracked 状态，所以是纯文件 mv + rm，不用 `git mv`。
- 新建 `docs/04-开发管理/README.md`（或追加到 `docs/README.md` 索引）让其它人能发现这 3 份文件。

---

### D-2026-06-04-08 — `.gitignore` 追加 3 类 IP / 隐私 pattern

**决策：** 追加以下 pattern 到 `.gitignore`：

```gitignore
# Third-party / internal-sensitive content (kept on disk, not on GitHub)
**/前端设计参考图/
**/tokenplan_api文档/
*-修改建议.md
```

**Why:**
- `**/前端设计参考图/`：5 张 Firecrawl 截图，是第三方设计参考，本项目不拥有版权也不应公开放。匹配 `**/` 前缀既覆盖根目录的、也覆盖任何深度的同名目录（防御未来同样的命名）。
- `**/tokenplan_api文档/`：4 个厂商的 API 文档（minimax / Xiaomi MiMo / Volcengine / 火山视频生成），版权通常归各厂商，公开放可能违反 ToS。同 `**/` 前缀防御。
- `*-修改建议.md`：捕捉"内部评估反馈"类型文件。当前只有 `minimax3-修改建议.md`，里面 P0 列了 API Key 明文等安全风险细节，公开发相当于把项目安全弱点广而告之。`*-修改建议.md` 是个宽 pattern，未来类似文件会自动被 ignore（防御性）。
- **不**用 `*修改建议.md`（去前导 `-`）—— 太宽，会误伤无关的"修改建议"。
- **不**用 `**/*.md` —— 显然太宽，会把整个项目的 md 文档全 ignore 掉。

**注意：** 这 3 个 pattern 之前未 ignore，所以 `minimax3-修改建议.md` 等目前还是 tracked 状态。Stage 5 需要 `git rm --cached` 把它们从索引里拿掉，但保留工作树文件。

---

### D-2026-06-04-09 — 暂不动 `.env`、`storage/`、其它已 ignore 的内容

**决策：** 现有 `.env` / `storage/*` / `node_modules/` 等 ignore 规则保持不动；本任务只追加 D-2026-06-04-08 列出的 3 条。

**Why:**
- 范围控制：本任务只针对用户明确点出的 3 类内容。
- `.env` 里如果有真实 API Key，是高危级别，但已经 ignore 了，加新 pattern 没意义。
- `storage/uploads/*` 等是运行时用户数据，已经 ignore 了。
- 不"顺手优化"无关 ignore 规则，符合 staged-execution 规则里"修改范围限制"那一条。
